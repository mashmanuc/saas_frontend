# Комплексний аудит календаря M4SH Platform v0.52
**Дата:** 25 грудня 2025  
**Аналітик:** AI Architecture Team  
**Мета:** Порівняльний аналіз поточної реалізації з Bukischool + план покращень

---

## 1. ПОТОЧНИЙ СТАН BACKEND

### 1.1 API Endpoints (що вже є)

#### ✅ Week Snapshot API
- **Endpoint:** `GET /api/v1/calendar/week`
- **Файл:** `backend/apps/booking/api/v1_calendar_week.py`
- **Що повертає:**
  - `days[]` — метадані днів тижня (dayKey, dow, label, day, month, year)
  - `events{}` — події згруповані по днях (ключ = dayKey)
  - `accessible{}` — доступні слоти по днях
  - `orders[]` — активні замовлення студентів
  - `meta` — статистика (totalHours, salary, billing)
  - `weekMeta` — інформація про тиждень

**Особливості:**
- ✅ ETag caching (304 Not Modified)
- ✅ Compression (Brotli/Gzip)
- ✅ Rate limiting (60 req/min)
- ✅ Opt-in payments/stats (`includePayments`, `includeStats`)
- ✅ Timezone support
- ✅ Page navigation (prev/current/next week)

**Структура даних:**
```json
{
  "status": "success",
  "data": {
    "weekMeta": { "weekStart": "2025-12-22", "weekEnd": "2025-12-28" },
    "days": [{ "dayKey": "2025-12-22", "dow": 0, "label": "Mon" }],
    "events": {
      "2025-12-22": [
        {
          "id": 123,
          "type": "class",
          "start": "2025-12-22T10:00:00+02:00",
          "end": "2025-12-22T11:00:00+02:00",
          "clientName": "Іван Петренко",
          "paidStatus": "paid"
        }
      ]
    },
    "accessible": {
      "2025-12-22": [
        {
          "id": 456,
          "type": "available_slot",
          "start": "2025-12-22T14:00:00+02:00",
          "end": "2025-12-22T16:00:00+02:00"
        }
      ]
    },
    "orders": [],
    "meta": { "totalHours": 14 }
  }
}
```

#### ✅ Slot CRUD API
- **Endpoints:**
  - `GET /api/v1/booking/slots/{id}/` — деталі слоту
  - `PUT /api/v1/booking/slots/{id}/` — редагування
  - `DELETE /api/v1/booking/slots/{id}/` — видалення
  - `POST /api/v1/booking/slots/batch-edit/` — групове редагування
  - `POST /api/v1/booking/slots/check-conflicts/` — перевірка конфліктів

**Файл:** `backend/apps/booking/api/views.py` (SlotDetailView, SlotBatchEditView, SlotConflictsView)

**Особливості:**
- ✅ Conflict detection
- ✅ Edit strategies (override, template_update, user_choice)
- ✅ Override reasons
- ✅ Throttling (rate limits)
- ✅ Permissions (IsTutor)

#### ✅ Event CRUD API
- `POST /api/v1/calendar/events/` — створення події
- `PUT /api/v1/calendar/events/{id}/` — оновлення
- `DELETE /api/v1/calendar/events/{id}/` — видалення
- `GET /api/v1/calendar/events/{id}/` — деталі

### 1.2 Backend Services

#### ✅ Week Snapshot Generator
**Файл:** `backend/apps/booking/services/week_snapshot_generator.py`

**Функції:**
- `generate_week_snapshot()` — генерує snapshot для тижня
- `generate_snapshot_etag()` — обчислює ETag для кешування
- `get_week_start()` — визначає понеділок тижня

**Що робить:**
1. Запитує події з БД (`CalendarEvent`)
2. Запитує доступні слоти (`TutorAvailabilitySlot`)
3. Запитує замовлення (`Order`)
4. Групує по днях
5. Обчислює статистику (години, зарплата)
6. Повертає нормалізовану структуру

#### ✅ Slot Editor Service
**Файл:** `backend/apps/booking/services/slot_editor.py`

**Функції:**
- `edit_single_slot()` — редагування одного слоту
- `edit_batch_slots()` — групове редагування
- `delete_slot()` — видалення слоту
- `check_conflicts()` — перевірка конфліктів

---

## 2. ПОТОЧНИЙ СТАН FRONTEND

### 2.1 Компоненти

#### ✅ CalendarWeekView.vue
**Шлях:** `frontend/src/modules/booking/components/calendar/CalendarWeekView.vue`

**Що робить:**
- Рендерить тижневий календар
- Показує навігацію по тижнях
- Відображає легенду (уроки + доступність)
- Інтегрує `CalendarBoard` для сітки

**Проблеми:**
- ❌ Немає inline-кнопок на слотах
- ❌ Немає фільтрів перегляду (тільки уроки / тільки доступність)
- ❌ Немає bulk-selection
- ❌ Немає sidebar зі списком найближчих уроків

#### ✅ CalendarBoard.vue
**Шлях:** `frontend/src/modules/booking/components/calendar/CalendarBoard.vue`

**Що робить:**
- Рендерить grid (7 днів × 48 слотів по 30 хв)
- Показує клітинки часу
- Overlay для івентів (`EventsOverlay`)
- Overlay для доступності (`AvailabilityOverlay`)

**Проблеми:**
- ❌ Немає drag-select для bulk-редагування
- ❌ Немає inline-дій на клітинках

#### ✅ AvailabilityEditor.vue
**Шлях:** `frontend/src/modules/booking/components/availability/AvailabilityEditor.vue`

**Що робить:**
- Редагування щотижневого розкладу
- Показує `DaySchedule` для кожного дня
- Undo/Redo функціонал
- Кнопка "Назад до календаря"

**Проблеми:**
- ❌ Не інтегрований `SlotEditor` для редагування окремих слотів
- ❌ Немає preview змін перед збереженням
- ❌ Немає bulk-операцій (копіювання тижня, шаблони)

#### ✅ SlotEditor.vue
**Шлях:** `frontend/src/modules/booking/components/availability/SlotEditor.vue`

**Що робить:**
- Модалка для редагування слоту
- TimeRangeInput для зміни часу
- ConflictResolver для конфліктів
- Стратегії редагування (override, template_update, user_choice)
- **НОВИНКА:** Кнопка видалення слоту

**Проблеми:**
- ❌ Не відкривається з календаря (не інтегрована)

### 2.2 Stores (Pinia)

#### ✅ calendarWeekStore.ts
**Шлях:** `frontend/src/modules/booking/stores/calendarWeekStore.ts`

**Що зберігає:**
- `weekMeta`, `days`, `meta`
- `eventsById`, `eventIdsByDay`, `allEventIds` (нормалізовані події)
- `accessibleById`, `accessibleIdsByDay`, `allAccessibleIds` (нормалізовані слоти)
- `ordersById`, `allOrderIds`
- `isLoading`, `error`, `selectedEventId`
- `etag` (для кешування)
- `optimisticUpdates` (для оптимістичних оновлень)

**Функції:**
- `fetchWeek()` — завантажує snapshot
- `createEvent()`, `updateEvent()`, `deleteEvent()`
- `selectEvent()`, `bulkUpdateEvents()`
- WebSocket handlers (для real-time оновлень)

**Проблеми:**
- ❌ Немає `undo/redo` для дій у календарі (тільки в редакторі)
- ❌ Немає bulk-selection state

### 2.3 API Clients

#### ✅ calendarWeekApi.ts
**Шлях:** `frontend/src/modules/booking/api/calendarWeekApi.ts`

**Функції:**
- `fetchWeekSnapshot()` — GET /api/v1/calendar/week
- ETag caching
- Compression support

#### ✅ booking.ts
**Шлях:** `frontend/src/modules/booking/api/booking.ts`

**Функції:**
- `editSlot()`, `deleteSlot()`, `batchEditSlots()`
- `checkSlotConflicts()`
- `createEvent()`, `updateEvent()`, `deleteEvent()`

---

## 3. ПОРІВНЯННЯ З BUKISCHOOL

### 3.1 Що є у Букі (з buki.md + скріншот)

#### ✅ Week Snapshot Pattern
- **Букі:** Один запит `/tutor/calendar/get?page=0` повертає все
- **M4SH:** ✅ Аналогічно `/api/v1/calendar/week?page=0`

#### ✅ Grid + Overlay Architecture
- **Букі:** Двошаровий календар (grid cells + draggable events)
- **M4SH:** ✅ Аналогічно (CalendarBoard + EventsOverlay + AvailabilityOverlay)

#### ✅ CRUD → Refetch Pattern
- **Букі:** Після delete/create робить refetch snapshot
- **M4SH:** ✅ Аналогічно (store.fetchWeek після мутацій)

#### ❌ Inline Actions
- **Букі:** Кнопки "+", "Редагувати" прямо на слотах
- **M4SH:** ❌ Немає (треба відкривати модалку окремо)

#### ❌ Visual Clarity
- **Букі:** Чітке розділення доступних/недоступних клітинок (сірі/білі)
- **M4SH:** ⚠️ Є, але менш виразно (жовтий overlay для доступності)

#### ❌ Compact UI
- **Букі:** Компактний вигляд, багато інформації на екрані
- **M4SH:** ⚠️ Більше відступів, менше щільність

---

## 4. ВИЯВЛЕНІ ПРОБЛЕМИ

### 4.1 UX/UI Issues

1. **Немає швидких дій на слотах**
   - Треба клікати → відкривати модалку → редагувати
   - У Букі: кнопки "+", "✏️" прямо на слоті

2. **Немає фільтрів перегляду**
   - Не можна приховати уроки/доступність окремо
   - Не можна згорнути вихідні

3. **Немає bulk-selection**
   - Не можна вибрати кілька слотів і змінити разом
   - У Букі: можна виділити drag-select

4. **Немає sidebar зі списком уроків**
   - Треба скролити календар, щоб побачити всі уроки
   - У Букі: праворуч список найближчих

5. **Легенда не інтерактивна**
   - Не можна клікнути на "Уроки" і приховати їх
   - У Букі: можна toggle шари

6. **Немає undo/redo у календарі**
   - Є тільки в AvailabilityEditor
   - Треба додати для дій у CalendarWeekView

7. **WebSocket помилки**
   - Показується помилка, якщо backend не запущений
   - Треба fallback або graceful degradation

8. **Немає "швидкого створення уроку"**
   - Треба відкривати модалку, заповнювати форму
   - У Букі: клік на слот → швидка форма

### 4.2 Architecture Issues

1. **SlotEditor не інтегрований у календар**
   - Існує компонент, але не відкривається з CalendarWeekView
   - Треба додати emit + modal wrapper

2. **Немає bulk-операцій у backend**
   - Є batch-edit для слотів, але немає для івентів
   - Треба додати bulk delete/move

3. **Немає шаблонів розкладу**
   - Не можна зберегти "типовий тиждень" і застосувати
   - Треба додати templates API + UI

4. **Немає drag-and-drop для івентів**
   - Події не можна перетягувати
   - Треба додати draggable logic

5. **Немає конфлікт-резолюшн у календарі**
   - Є тільки в SlotEditor
   - Треба показувати конфлікти при створенні події

---

## 5. КОМПЛЕКСНИЙ ПЛАН ПОКРАЩЕНЬ

### 5.1 Priority 0 (Critical UX)

#### P0.1: Інтеграція SlotEditor у календар
**Мета:** Клік на доступний слот → відкривається SlotEditor

**Зміни:**
- `CalendarBoard.vue`: додати `@click` на `AvailabilityOverlay`
- `CalendarWeekView.vue`: додати `<SlotEditorModal>` + state для відкриття
- Emit `slot-clicked` → відкрити модалку з даними слоту

**Ризики:** Низькі (компонент вже є, треба тільки прив'язати)

#### P0.2: Inline-кнопки на слотах
**Мета:** Швидкі дії без відкриття модалки

**Зміни:**
- `AvailabilityOverlay.vue`: додати кнопки "+", "✏️", "🗑️" при hover
- CSS: позиціонувати кнопки в правому верхньому куті слоту
- Логіка: "+" → створити урок, "✏️" → редагувати, "🗑️" → видалити

**Ризики:** Середні (треба продумати UX для мобільних)

#### P0.3: Фільтри перегляду
**Мета:** Toggle для показу уроків/доступності

**Зміни:**
- `CalendarWeekView.vue`: додати чекбокси "Показати уроки", "Показати доступність"
- `calendarWeekStore.ts`: додати `showEvents`, `showAccessible` state
- `CalendarBoard.vue`: умовний рендер `EventsOverlay` / `AvailabilityOverlay`

**Ризики:** Низькі

#### P0.4: Sidebar зі списком уроків
**Мета:** Показувати найближчі уроки праворуч

**Зміни:**
- `CalendarWeekView.vue`: додати `<CalendarSidebar>` компонент
- `CalendarSidebar.vue`: список уроків з `eventsById`, сортування по `start`
- CSS: fixed sidebar праворуч, scroll якщо багато

**Ризики:** Низькі

### 5.2 Priority 1 (Enhanced UX)

#### P1.1: Bulk-selection
**Мета:** Drag-select кількох слотів

**Зміни:**
- `CalendarBoard.vue`: додати drag-select logic (mousedown → mousemove → mouseup)
- `calendarWeekStore.ts`: додати `selectedSlotIds[]` state
- UI: виділені слоти підсвічуються
- Кнопки: "Видалити всі", "Змінити час всіх"

**Ризики:** Високі (складна логіка, треба тестувати)

#### P1.2: Drag-and-drop для івентів
**Мета:** Перетягування уроків між слотами

**Зміни:**
- `EventBlock.vue`: додати `draggable="true"` + `@dragstart`, `@dragend`
- `CalendarBoard.vue`: додати `@drop` на клітинки
- Backend: API для переміщення події (PUT /api/v1/calendar/events/{id}/move)
- Conflict detection: перевіряти перед drop

**Ризики:** Високі (треба backend API, conflict logic)

#### P1.3: Undo/Redo у календарі
**Мета:** Скасування дій у CalendarWeekView

**Зміни:**
- `calendarWeekStore.ts`: додати `undoStack`, `redoStack`
- Команди: `CreateEventCommand`, `DeleteEventCommand`, `MoveEventCommand`
- UI: кнопки Undo/Redo у верхній панелі

**Ризики:** Середні (є вже в AvailabilityEditor, можна переюзати)

#### P1.4: Шаблони розкладу
**Мета:** Зберегти "типовий тиждень" і застосувати

**Зміни:**
- Backend: API для збереження/завантаження шаблонів
  - `POST /api/v1/booking/templates/` — створити шаблон
  - `GET /api/v1/booking/templates/` — список шаблонів
  - `POST /api/v1/booking/templates/{id}/apply` — застосувати
- Frontend: `<TemplateManager>` компонент
- UI: кнопка "Зберегти як шаблон", dropdown "Застосувати шаблон"

**Ризики:** Високі (треба нова таблиця в БД, backend logic)

### 5.3 Priority 2 (Polish)

#### P2.1: Інтерактивна легенда
**Мета:** Клік на "Уроки" → приховати уроки

**Зміни:**
- `CalendarWeekView.vue`: додати `@click` на `.legend-item`
- Toggle `showEvents` / `showAccessible`

**Ризики:** Низькі

#### P2.2: WebSocket fallback
**Мета:** Graceful degradation якщо WebSocket не працює

**Зміни:**
- `useCalendarWebSocket.ts`: додати fallback на polling
- Якщо WebSocket fails → переключитися на `setInterval(() => store.fetchWeek(), 30000)`
- UI: показувати статус з'єднання (зелена/червона крапка)

**Ризики:** Низькі

#### P2.3: Швидке створення уроку
**Мета:** Клік на порожню клітинку → швидка форма

**Зміни:**
- `CalendarBoard.vue`: додати `@click` на порожні клітинки
- `<QuickEventForm>`: мінімальна форма (студент, час, тривалість)
- Після створення → refetch snapshot

**Ризики:** Середні (треба UX для вибору студента)

#### P2.4: Compact mode
**Мета:** Зменшити відступи, збільшити щільність

**Зміни:**
- CSS: зменшити padding у `.calendar-board`, `.cell`
- Зменшити висоту клітинок (з 60px до 40px)
- Зменшити шрифти

**Ризики:** Низькі (тільки CSS)

---

## 6. ОЦІНКА РИЗИКІВ

### 6.1 Технічні ризики

1. **Bulk-selection + Drag-and-drop**
   - Складна логіка, багато edge cases
   - Треба тестувати на різних розмірах екрану
   - Може конфліктувати з існуючим drag-select

2. **Шаблони розкладу**
   - Треба нова таблиця в БД
   - Треба міграція даних
   - Треба продумати, як зберігати шаблон (JSON? Окремі записи?)

3. **Drag-and-drop для івентів**
   - Треба backend API для переміщення
   - Треба conflict detection
   - Треба оптимістичні оновлення (щоб не було затримки)

### 6.2 UX ризики

1. **Перевантаження UI**
   - Якщо додати багато кнопок → буде захаращено
   - Треба продумати, коли показувати inline-кнопки (тільки при hover?)

2. **Мобільна версія**
   - Drag-and-drop не працює на тачскрінах
   - Bulk-selection складно реалізувати на мобільних
   - Треба окремий UX для мобільних

3. **Продуктивність**
   - Якщо багато слотів (100+) → може лагати рендер
   - Треба віртуалізація або lazy loading

---

## 7. РЕКОМЕНДАЦІЇ

### 7.1 Короткострокові (1-2 тижні)

1. **P0.1-P0.4** — реалізувати критичні UX покращення
2. **P2.2** — виправити WebSocket fallback
3. **Додати i18n ключі** — для всіх нових UI елементів

### 7.2 Середньострокові (1 місяць)

1. **P1.1-P1.3** — bulk-selection, drag-and-drop, undo/redo
2. **P2.3-P2.4** — швидке створення, compact mode
3. **E2E тести** — для нових фіч

### 7.3 Довгострокові (2-3 місяці)

1. **P1.4** — шаблони розкладу
2. **Мобільна версія** — окремий UX для тачскрінів
3. **Аналітика** — dashboard для статистики

---

## 8. ВИСНОВКИ

### Що вже добре реалізовано:
- ✅ Week Snapshot API (аналогічно Букі)
- ✅ Grid + Overlay архітектура
- ✅ ETag caching, compression, rate limiting
- ✅ Slot CRUD API з conflict detection
- ✅ Undo/Redo в AvailabilityEditor
- ✅ Легенда з кольорами

### Що треба покращити:
- ❌ Inline-дії на слотах (як у Букі)
- ❌ Фільтри перегляду
- ❌ Bulk-selection
- ❌ Sidebar зі списком уроків
- ❌ Інтеграція SlotEditor у календар
- ❌ Drag-and-drop для івентів
- ❌ Шаблони розкладу

### Пріоритет:
1. **P0** (критичні UX) — 1-2 тижні
2. **P1** (enhanced UX) — 1 місяць
3. **P2** (polish) — 2-3 місяці

### Наступні кроки:
1. Реалізувати P0.1-P0.4 без переривання
2. Додати i18n ключі
3. Написати E2E тести
4. Отримати feedback від користувачів
5. Ітерувати на основі feedback

---

**Підготував:** AI Architecture Team  
**Затверджено до реалізації:** ✅
