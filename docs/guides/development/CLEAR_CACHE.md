# Скрипт очищення кешу для тестування виправлень

## Після виправлення коду потрібно очистити кеш браузера

Виконайте цей скрипт у DevTools Console (F12 → Console) на сторінці http://127.0.0.1:5173:

```javascript
(function clearMarketplaceCache() {
  console.log('🧹 Очищення кешу Marketplace...');
  
  const keysToRemove = [];
  const patterns = [
    /^marketplace_filters_cache/i,
    /^marketplace_ext_filters_cache/i,
    /^catalogVersion$/i,
    /^subjectTagMap$/i,
    /^marketplace_subject_tab_mode/i,
  ];

  // Збираємо всі ключі що відповідають патернам
  for (let i = 0; i < localStorage.length; i++) {
    const key = localStorage.key(i);
    if (key && patterns.some((regex) => regex.test(key))) {
      keysToRemove.push(key);
    }
  }

  // Видаляємо знайдені ключі
  keysToRemove.forEach((key) => {
    localStorage.removeItem(key);
    console.log(`  ❌ Видалено: ${key}`);
  });

  console.log(`✅ Очищено ${keysToRemove.length} ключів кешу`);
  console.log('🔄 Перезавантаження сторінки...');
  
  // Перезавантажуємо сторінку для застосування змін
  setTimeout(() => window.location.reload(), 500);
})();
```

## Альтернативний метод (якщо скрипт не працює)

1. Відкрийте DevTools (F12)
2. Перейдіть на вкладку **Application** (або **Програма**)
3. У лівій панелі виберіть **Local Storage** → **http://127.0.0.1:5173**
4. Знайдіть та видаліть ключі:
   - `marketplace_filters_cache:*` (всі варіанти)
   - `marketplace_ext_filters_cache:*` (всі варіанти)
   - `catalogVersion`
   - `subjectTagMap`
   - `marketplace_subject_tab_mode_v1`
5. Перезавантажте сторінку (F5 або Ctrl+R)

## Що було виправлено

### 1. Теги предметів
- ✅ Виправлено нормалізацію груп тегів (grades→classes, formats→format)
- ✅ Тепер теги відображаються для всіх предметів

### 2. Валідація мови викладання
- ✅ Валідація спрацьовує тільки після взаємодії користувача
- ✅ Не показує помилки одразу при завантаженні сторінки

### 3. Прогрес-бар профілю
- ✅ Тепер перевіряє `teaching_languages` замість deprecated `languages`
- ✅ Коректно відображає реальний стан заповнення профілю

## Перевірка після очищення кешу

1. **Залогінитись:** m5@gmail.com / demo1234
2. **Перейти:** Dashboard → Профіль тьютора → Редагування
3. **Перевірити теги:**
   - Вибрати предмет (Фізика, Хімія, Математика)
   - Мають відображатися вкладки з тегами: Класи, Іспити, Цілі, Формат, Аудиторія
4. **Перевірити валідацію:**
   - Вкладка "Мова викладання"
   - Помилка НЕ має відображатися одразу
   - Помилка з'являється тільки після додавання/видалення мови
5. **Перевірити прогрес-бар:**
   - Прогрес має відповідати реальному стану профілю

## Stress-тест

Виконати повний цикл:
1. Опублікувати профіль на маркетплейсі
2. Видалити профіль
3. Повторно опублікувати профіль
4. Перевірити що всі дані (теги, мови, прогрес) збереглися

---

**Дата виправлень:** 01.02.2026  
**Версія:** v0.86 FIX
