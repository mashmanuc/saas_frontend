PROMPT ДЛЯ АГЕНТА (E2E QA + FIX: Subjects/Tags → Marketplace)

ВАЖЛИВО: сервери ВЖЕ ЗАПУЩЕНІ. НІЧОГО не запускай і не зупиняй.
НЕ роби міграцій, НЕ сійди базу, НЕ чисти БД.
Працюй лише з існуючим кодом та UI-багами відображення.

0) Середовище (НЕ чіпати)
Frontend вже запущений:

URL: http://127.0.0.1:5173/

Vite dev server already running

Backend вже запущений
Celery вже запущений

Команда (лише для інформації, НЕ запускати):

python -m celery -A config worker --pool=solo --loglevel=info

Backend використовує virtualenv

Якщо тобі треба запускати будь-які python-команди (лише якщо критично) — роби через:

.venv\Scripts\python.exe (Windows)

Але в ідеалі взагалі не запускай нічого, працюй через сайт. 

1) Вхід на сайтчерез Puppeteer

Відкрий http://127.0.0.1:5173/

Дочекайся повного завантаження сторінки

Залогінься:

Login: m3@gmail.com
Password: demo1234

Після логіну обовʼязково переключись з мобільної версії на десктопну (якщо активна мобільна верстка)

2) Перевірка Billing (швидка точка контролю)

Перейди в Tutor Profile




3) Основна ціль: Tutor Profile → Subjects/Tags (повний цикл)
3.1 Відкрити профіль тьютора



Знайди вкладку "Предмети"

3.2 Перевірити “відображення” (UI correctness)

Твоя задача: перевірити чи те, що показано в UI = те, що реально збережено і повертається бекендом.

Перевірити, що:

вибрані предмети показуються коректно

теги по предметах показуються коректно

немає “чужих тегів”, які не належать предмету

немає пустих або дубльованих тегів

після save дані не злітають

3.3 Звірка з реальністю (DB/API truth check)
не спіши , часу вистачає 
 перевір різні предмети та мови
Потрібно звірити UI з тим, що реально є в системі:

відкрий DevTools → Network

знайди запити які повертають профіль/subjects/tags/relations

порівняй payload з тим, що показано у вкладці

Якщо у UI показано одне, а в API приходить інше — це BUG UI mapping.
Якщо в API приходить неправильне — це BUG бекенда / контракту / серіалізатора.

Ти маєш визначити де джерело проблеми і виправити.

4) Тестова дія: обрати предмети/теги → зберегти → опублікувати

Зроби реальний сценарій:

Обери 3–5 предмети

Для кожного предмета:

вистав теги (цілі/формат/аудиторія/екзамени/рівні — що доступно в UI)
(перевір чи теги відображаються коректно і виправ , якщо не відображаються згідно  з планом)
Натисни Save / Зберегти

Перезавантаж сторінку (hard refresh) і переконайся:

вибране НЕ зникло

відображення після reload коректне

Якщо є кнопка Publish / Опублікувати профіль — опублікуй

5) Marketplace перевірка (обов’язково)

Після того як предмети/теги збережені:

Перейди в Marketplace

Знайди себе (цього тьютора) у списку

Відкрий профіль тьютора як його бачить учень

Переконайся що на Marketplace відображається:

ті самі предмети

ті самі теги

коректні назви і локалізація

НІЧОГО не пропало після публікації

6) Якщо є проблеми — фіксити НЕ зупиняючись

Якщо на будь-якому кроці знайдено баг:

✅ Ти зобов’язаний:

локалізувати причину (Frontend/Backend/Contract)

виправити мінімально і правильно

перевірити, що після фіксу:

Tutor Profile показує правильно

Save працює

Reload не ламає

Marketplace показує те саме

🚫 Заборонено:

“тимчасові хаки”

“відключити перевірку”

“замість фіксу сховати елемент”

ламати контракт

✅ Definition of Done (коли задача виконана)

Задача вважається виконаною ТІЛЬКИ якщо:

У вкладці Tutor Profile → Предмети

предмети і теги показуються коректно

можна змінити і зберегти

після reload все лишається як було

У Marketplace

показано ті самі предмети і теги

вибрані значення відповідають реальним даним (API truth)

По дорозі:

всі знайдені критичні баги були пофікшені

після фіксу проведено повторний прогін перевірки

Формат звіту (після завершення)

В кінці ти маєш написати коротко:

✅ що було перевірено (які кроки)

🐞 які баги були знайдені

🔧 що саме виправлено (файли/місця)

✅ підтвердження що Marketplace = Profile = API