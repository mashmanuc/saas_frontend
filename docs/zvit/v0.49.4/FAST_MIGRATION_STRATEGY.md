# ⚡ Fast Migration Strategy v0.49.4

**Мета:** Швидко завершити міграцію, дотримуючись контракту

---

## 🎯 Стратегія

### Група A: Видалити без міграції (legacy UI)
Ці компоненти — частина старого UI, який вже замінено на CalendarWeekView:

1. ✅ **CalendarCellGrid.vue** — замінено на CalendarWeekView
2. ✅ **DraftToolbar.vue** — функціонал не потрібен в новому календарі
3. ✅ **useCalendar.ts** — обгортка над calendarStore

**Дія:** Видалити одразу після міграції залежностей

---

### Група B: Замінити імпорт (прості зміни)
Ці модулі просто імпортують calendarStore, але не використовують його активно:

4. ✅ **bookingStore.ts** — видалити імпорт calendarStore
5. ✅ **BookingModal.vue** — замінити на calendarWeekStore або видалити залежність

**Дія:** Видалити імпорт або замінити на calendarWeekStore

---

### Група C: Потребують окремого API (не calendar)
Ці модулі використовують calendarStore для availability/settings, а не для календаря:

6. ✅ **AvailabilityEditor.vue** (booking) — використовує availabilityApi
7. ✅ **AvailabilityEditor.vue** (matches) — використовує availabilityApi
8. ✅ **ExceptionManager.vue** — використовує bookingApi для exceptions
9. ✅ **BookingSettings.vue** — використовує bookingApi для settings
10. ✅ **SlotPicker.vue** — використовує availabilityApi або calendarWeekStore

**Дія:** Замінити calendarStore на прямі виклики API

---

### Група D: Booking flow (критичні)
Ці модулі — частина booking flow, потребують ретельної міграції:

11. ✅ **BookingRequestsView.vue** — використовує bookingApi для requests
12. ✅ **BookLessonView.vue** — студент бронює урок, використовує публічний API

**Дія:** Перевірити що використовують bookingApi, а не calendarStore

---

### Група E: Legacy stores (видалити останніми)
13. ✅ **calendarStore.ts** — видалити після всіх залежностей
14. ✅ **draftStore.ts** — видалити разом з calendarStore

---

## 🚀 План виконання

### Крок 1: Перевірити залежності (5 хв)
```bash
grep -r "useCalendarStore" src/ --include="*.vue" --include="*.ts"
```

### Крок 2: Група B — Прості зміни (10 хв)
- bookingStore.ts — видалити імпорт
- BookingModal.vue — перевірити чи використовується

### Крок 3: Група C — Availability модулі (15 хв)
- Замінити calendarStore на прямі API виклики
- Використовувати availabilityApi, bookingApi

### Крок 4: Група D — Booking flow (10 хв)
- Перевірити що використовують bookingApi
- Якщо використовують calendarStore — замінити

### Крок 5: Група A — Видалити legacy UI (5 хв)
- CalendarCellGrid.vue
- DraftToolbar.vue
- useCalendar.ts

### Крок 6: Група E — Видалити stores (5 хв)
- calendarStore.ts
- draftStore.ts
- types/calendar.ts

### Крок 7: Typecheck + Build (5 хв)
```bash
npm run typecheck
npm run build
```

**Загальний час:** ~55 хвилин

---

## ✅ Прогрес

- ✅ Група A: 0/3
- ✅ Група B: 0/2
- ✅ Група C: 0/5
- ✅ Група D: 0/2
- ✅ Група E: 0/3

**Загалом:** 0/15

---

**Наступний крок:** Група B — bookingStore.ts
