📦 Архітектура проєкту
Версія: v0.3.0

Цей документ описує архітектурні принципи, компоненти та взаємодії бекенду й фронтенду на етапі версії 0.3.0. Він є фундаментом, на який спиратимуться наступні релізи (v0.4.0+), а також база для майбутніх продуктів на платформі.

1. Загальне бачення архітектури
Коротко:

Бекенд: Django + DRF + SimpleJWT (HttpOnly refresh-cookie)

Фронтенд: Vue 3 + Pinia + Vue Router + Tailwind + i18n + власна Design System

Аутентифікація: Access Token у JSON + Refresh Token у HttpOnly cookie

Ролі користувачів: student, tutor, admin, superadmin

Модель навчання: self-learning + багатотьюторність (пасивні звʼязки)

Принципи:

Строга розділеність відповідальностей.

Уніфіковані API-контракти.

Повністю централізована авторизація й робота з токенами.

Уніфікована дизайн-система з темною/світлою темами.

Стійкість до розширення для майбутніх продуктів.

2. Архітектура бекенду
2.1 Технологічна база

Python 3 + Django

Django REST Framework

SimpleJWT (оновлена схема refresh-cookie)

corsheaders

Архітектура REST-first з чіткими ресурсами.

2.2 Модельні сутності
User
Поле	Опис
role	student / tutor / admin / superadmin
is_self_learning	true — студент не прикріплений до тьютора
timezone	Часовий пояс користувача
email, first_name, last_name	Базові дані
TutorProfile

Публічна інформація тьютора:

bio

subjects

hourly_rate

country

rating

is_public

TutorStudentRelation

Серце зв’язків студент ↔ тьютор.

Поле	Опис
student	User (role=student)
tutor	User (role=tutor)
status	invited / active / paused / archived
notes	Додаткова інформація
invited_by	хто ініціював звʼязок
relationship_start, relationship_end	робочі дати
created_at, updated_at	технічні дати
Особливість (v0.3.0):

✔ Студент може мати багато тьюторів, але тільки один ACTIVE одночасно.
✔ У профілі зберігаються всі минулі й майбутні тьютори.

3. Архітектура фронтенду
3.1 Технологічна база

Vue 3 (Composition API)

Pinia (stores: auth, loader, notify, settings, theme, dashboard)

Vue Router

TailwindCSS + власна Design System

vue-i18n

Axios з централізованим apiClient

3.2 Design System (v0.1.0 → інтегрована в v0.3.0)

Власна уніфікована система стилів:

Компоненти:

Button

Card

Input

MenuItem

SideNav

TopNav

ToastContainer (notify system)

GlobalLoader

PageShell

Теми:

light

dark

system

CSS Variables:

--bg-primary, --text-primary, --border-color, --card-bg, --nav-bg, --toast-bg, --accent, --success-bg, ...
→ Повністю адаптивні та масштабовані.

4. Аутентифікація
4.1 Потік авторизації
Login/Register/Invite Accept

backend повертає access у JSON

backend ставить refresh у HttpOnly cookie

фронт зберігає access у Pinia + localStorage

після login/register/invite фронт викликає reloadUser()

Refresh Flow

axios interceptor:

ловить 401

виконує /auth/refresh/

повторює запит

при помилці виконує forceLogout()

✔ Повна відповідність безпечним стандартам.

5. Авторизація за ролями
5.1 getDefaultRouteForRole()
Роль	Route
student	/student
tutor	/tutor
admin	/admin
superadmin	/superadmin
5.2 hasAccess()

Router guard перевіряє:

чи є токен

чи має користувач доступ до маршруту

чи потрібно редіректити

6. Навчальна логіка
6.1 Студент self-learning

Після реєстрації:

role = student
is_self_learning = true


У Student Dashboard:

показується блок «Ви навчаєтесь самостійно»

доступний пошук тьюторів

можна подати запит тьютору

6.2 Студент → Тьютор
POST /student/request_tutor/

створює TutorStudentRelation зі статусом invited

студент стає is_self_learning = false

6.3 Тьютор → Прийняття студента
POST /tutor/accept_request/

змінює статус invited → active

встановлює relationship_start

студент гарантовано is_self_learning = false

6.4 Тьютор Dashboard

список студентів

вкладка «Запити студентів»

кнопка «Підтвердити»

7. Пошук тьюторів
GET /tutors/public-list/

Повертає:

full_name

subjects

rating

hourly_rate

bio

timezone

country

Фронтенд:

TutorSearchView.vue

список тьюторів

кнопка «Подати запит»

8. Фронтенд маршрути та сторінки
Route	Опис
/auth/*	login, register, invite
/student	dashboard + пошук тьюторів
/tutor	dashboard + студентські запити
/classrooms	зарезервовано
/lessons	зарезервовано

Всі сторінки в PageShell з темами, toast і loader.

9. Міжнародна підтримка

Файли локалей:

src/i18n/locales/uk.json

src/i18n/locales/en.json

Повна локалізація:

auth

dashboard

tutor search

tutor requests

notifications

theme switcher

menu items

10. Масштабованість архітектури (чому підходить для майбутніх продуктів)
Архітектура вже підтримує:
1) Освітню платформу з репетиторами (поточний проєкт)

Всі звʼязки, ролі, запити — готові.

2) Платформа для групових занять / класрумів

TutorStudentRelation легко розширюється до Classroom → Members.

3) Онлайн-курси / самоосвіта

self-learning → готова модель.

4) Маркетплейс репетиторів

public-list → основа для каталогу.

5) B2B-рішення (школи, навчальні центри)

архітектура з ролями дозволяє додати:

school admin

organizations

group dashboards

6) AI-репетитор / автоматичний тренажер

Достатньо додати endpoint і модуль AI.

11. Структура проєкту
🔹 Frontend
src/
  modules/
    auth/
    dashboard/
    tutor/
    student/
  stores/
  ui/
  utils/
  i18n/
  router/
  assets/

🔹 Backend
apps/
  users/
  tutors/
  relations/
  auth/
config/

12. Висновок щодо архітектури v0.3.0

✔ Архітектура модульна
✔ Повністю безпечна (HttpOnly refresh)
✔ Масштабована (готова до нових ролей, курсів, classroom)
✔ Мультимовна (uk + en)
✔ Має власну дизайн-систему
✔ Підтримує self-learning та взаємодії студент ↔ тьютор
✔ Готова показувати прогрес, уроки, класи, домашки
✔ Синхронізована з бекендом без розбіжностей