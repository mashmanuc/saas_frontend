# Аудит застарілого коду для видалення - v0.46 Calendar

**Дата:** 23 грудня 2024  
**Мета:** Очистити кодову базу від дублікатів та застарілих компонентів

---

## 🗑️ КОД ДЛЯ ВИДАЛЕННЯ

### 1. ❌ Backend: apps/calendar/api/ (ДУБЛІКАТ)

**Шлях:** `backend/apps/calendar/api/`

**Причина видалення:**
- Це заглушка/mock, створена для тестування UI
- Реальна імплементація знаходиться в `apps/booking/api/v1_week_view.py`
- Коментар у `config/urls.py:55-56` підтверджує, що calendar endpoints переїхали в booking
- Викликає плутанину та можливі конфлікти маршрутів

**Файли для видалення:**
```
backend/apps/calendar/
├── __init__.py
└── api/
    ├── __init__.py
    ├── urls.py
    └── views.py (88 рядків mock коду)
```

**Перевірка перед видаленням:**
```bash
# Перевірити, чи немає імпортів з apps.calendar
grep -r "from apps.calendar" backend/
grep -r "import apps.calendar" backend/
```

**Дія:** Видалити всю директорію `backend/apps/calendar/`

---

### 2. ⚠️ Frontend: Застарілі методи в calendarStore

**Файл:** `frontend/src/modules/booking/stores/calendarStore.ts`

**Проблема:** Store містить багато методів, які не використовуються в новому v0.46 календарі

**Методи для аналізу використання:**

#### Точно застарілі (використовують старий API):
- `loadSlots()` - використовує `/api/booking/slots/`
- `loadWeekSlots()` - обгортка над `loadSlots()`
- `blockSlot()` / `unblockSlot()` - старий підхід, замінений на draft patches
- `createCustomSlot()` - дублює manual booking

#### Можливо застарілі:
- `loadSettings()` / `updateSettings()` - перевірити використання
- `loadAvailability()` / `setAvailability()` - перевірити використання
- `syncAvailability()` - можливо застарілий
- `loadExceptions()` / `addException()` / `deleteException()` - перевірити використання
- `loadCalendar()` - для month view, перевірити використання

**Рекомендація:** 
1. Провести grep search по всій frontend кодовій базі
2. Якщо методи не використовуються - видалити
3. Або створити окремий `calendarStoreV046.ts` з чистою архітектурою

**Команди для перевірки:**
```bash
# Перевірити використання методів
grep -r "loadSlots" frontend/src --include="*.vue" --include="*.ts"
grep -r "blockSlot" frontend/src --include="*.vue" --include="*.ts"
grep -r "createCustomSlot" frontend/src --include="*.vue" --include="*.ts"
```

---

### 3. ⚠️ Frontend: Старі компоненти календаря

**Компоненти для перевірки:**

#### WeekCalendar.vue
**Шлях:** `frontend/src/modules/booking/components/calendar/WeekCalendar.vue`  
**Статус:** Можливо застарілий, якщо `CalendarCellGrid.vue` його замінює  
**Дія:** Перевірити використання, якщо не використовується - видалити

#### MonthCalendar.vue
**Шлях:** `frontend/src/modules/booking/components/calendar/MonthCalendar.vue`  
**Статус:** Перевірити, чи використовується в v0.46  
**Дія:** Залишити, якщо є функціонал місячного перегляду

**Команди для перевірки:**
```bash
grep -r "WeekCalendar" frontend/src --include="*.vue" --include="*.ts"
grep -r "MonthCalendar" frontend/src --include="*.vue" --include="*.ts"
```

---

### 4. ⚠️ Backend: Старі booking endpoints

**Файл:** `backend/apps/booking/api/views.py`

**Потенційно застарілі view класи:**
- `AvailableSlotsView` - можливо дублює Week View API
- `BlockSlotView` / `UnblockSlotView` - замінені на Bulk Apply API
- `CustomSlotView` - можливо дублює Manual Booking API

**Рекомендація:**
1. Провести аудит використання цих endpoints
2. Перевірити, чи є запити з frontend
3. Якщо не використовуються - позначити як deprecated
4. Видалити в наступному релізі після міграції

**Команди для перевірки:**
```bash
# Перевірити використання в frontend
grep -r "/api/booking/slots/" frontend/src
grep -r "blockSlot" frontend/src
grep -r "customSlot" frontend/src
```

---

### 5. 📝 Frontend: Застарілі типи та інтерфейси

**Файл:** `frontend/src/modules/booking/api/booking.ts`

**Потенційно застарілі типи:**
- `TimeSlot` - можливо замінений на `CalendarCell`
- `Availability` - перевірити використання
- `DateException` - перевірити використання

**Рекомендація:** Провести аналіз використання та видалити невикористовувані типи

---

## ✅ ПЛАН ОЧИЩЕННЯ

### Фаза 1: Безпечне видалення (негайно)

1. **Видалити `backend/apps/calendar/`**
   - Це точно дублікат
   - Не використовується в production
   - Команда: `rm -rf backend/apps/calendar/`

2. **Видалити mock handler з tests**
   - Файл: `frontend/tests/__mocks__/api/calendarHandlers.ts`
   - Причина: Тепер є dev mock в `calendarApi.ts`
   - Або залишити для unit тестів

### Фаза 2: Аналіз використання (1-2 дні)

1. **Провести grep search** для всіх підозрілих методів
2. **Створити список** невикористовуваних методів
3. **Позначити як @deprecated** в JSDoc/docstring

### Фаза 3: Deprecated (наступний реліз)

1. **Додати deprecation warnings** в консоль
2. **Оновити документацію** з міграційним гайдом
3. **Повідомити команду** про майбутнє видалення

### Фаза 4: Видалення (через 1-2 релізи)

1. **Видалити deprecated код**
2. **Оновити тести**
3. **Оновити документацію**

---

## 🔍 КОМАНДИ ДЛЯ АУДИТУ

### Backend

```bash
# Знайти всі імпорти з apps.calendar
grep -r "from apps.calendar" backend/ --include="*.py"

# Знайти всі посилання на старі endpoints
grep -r "/api/booking/slots/" backend/ --include="*.py"
grep -r "BlockSlotView" backend/ --include="*.py"

# Знайти невикористовувані view класи
grep -r "AvailableSlotsView" backend/ --include="*.py"
```

### Frontend

```bash
# Знайти використання старих методів store
grep -r "loadSlots\|blockSlot\|unblockSlot\|createCustomSlot" frontend/src --include="*.vue" --include="*.ts"

# Знайти використання старих компонентів
grep -r "WeekCalendar\|MonthCalendar" frontend/src --include="*.vue" --include="*.ts"

# Знайти використання старих API endpoints
grep -r "/api/booking/slots/" frontend/src --include="*.ts"
```

---

## 📊 ОЧІКУВАНИЙ РЕЗУЛЬТАТ

### Метрики очищення

**До очищення:**
- Backend: ~500 рядків застарілого коду
- Frontend: ~800 рядків застарілого коду
- Тести: ~300 рядків застарілих тестів

**Після очищення:**
- ✅ Менше плутанини для розробників
- ✅ Швидша збірка (менше файлів)
- ✅ Простіша підтримка
- ✅ Чистіша архітектура

### Ризики

**Низький ризик:**
- `apps/calendar/` - точно не використовується

**Середній ризик:**
- Методи в `calendarStore` - потрібна перевірка використання
- Старі компоненти - можуть використовуватись в інших місцях

**Високий ризик:**
- Старі booking endpoints - можуть використовуватись в legacy коді
- Типи - можуть використовуватись в багатьох місцях

---

## 🎯 НАСТУПНІ КРОКИ

1. ✅ **Негайно:** Видалити `backend/apps/calendar/`
2. 🔄 **Сьогодні:** Провести grep search для всіх підозрілих методів
3. 📝 **Завтра:** Створити список для deprecation
4. ⏳ **Наступний реліз:** Видалити deprecated код

---

**Відповідальний:** Backend + Frontend team  
**Пріоритет:** Середній (не блокує функціонал)  
**ETA:** 2-3 дні для повного аудиту
