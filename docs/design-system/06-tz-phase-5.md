# ТЗ — Фаза 5: Поступова міграція модулів

> Тривалість: 5-7 днів
> Залежності: Фази 1-4 (всі компоненти і CSS готові)
> Ризик зламу: середній (змінюємо HTML в модулях — кожен модуль = окремий коміт)

---

## Мета

Модуль за модулем замінити сирі HTML-елементи на UI-компоненти. Кожен модуль — окремий коміт, окремий деплой. Якщо щось зламалось — відкат одного коміту.

---

## Стратегія

Для кожного модуля виконати ці кроки:

1. **Кнопки:** Замінити сирі `<button class="btn ...">` → `<Button variant="..." size="...">`
2. **Textarea:** Замінити сирі `<textarea class="form-control">` → `<Textarea v-model="..." />`
3. **Модалки:** Замінити кастомні overlay → `<Modal :open="..." @close="...">`
4. **Форми:** Замінити `.form-group > label + input` → `<FormField>` або залишити з глобальними CSS
5. **Кольори:** Замінити хардкоджені кольори на CSS-змінні
6. **Тест:** Перевірити в 3 темах

---

## Порядок міграції

### Хвиля 1 — Видно студенту (пріоритет 🔴)

#### 5.1. Модуль `inquiries/` (~15 кнопок, 3 модалки)

**Файли:**
- `InquiryFormModal.vue` — головна модалка запиту
- `InquiryRejectModal.vue` — відхилення запиту
- `CreateInquiryButton.vue` — кнопка створення
- `InquiryCard.vue` — картка запиту
- `StudentInquiriesList.vue` — список запитів

**Зміни:**
- [ ] `InquiryFormModal.vue`: кастомний overlay → `<Modal>`
- [ ] Сирі `<button class="btn">` → `<Button>`
- [ ] `<textarea class="form-control">` → `<Textarea>`
- [ ] `.form-group` → `<FormField>` або залишити (CSS глобалізований)
- [ ] Перевірити 3 теми

---

#### 5.2. Модуль `dashboard/` (~20 кнопок, 0 модалок)

**Файли:**
- `DashboardStudent.vue`
- `DashboardTutor.vue`
- `StudentActiveTutorsSection.vue`
- `TutorDashboardStats.vue`
- Інші секції

**Зміни:**
- [ ] Сирі `<button>` → `<Button>`
- [ ] Перевірити 3 теми

---

#### 5.3. Модуль `auth/` (~30 кнопок, 4 модалки)

**Файли:**
- `LoginView.vue` / `RegisterView.vue`
- `MFAModal.vue` / `WebAuthnModal.vue`
- `BackupCodesModal.vue`
- `ForgotPasswordView.vue`
- `ContactUnlockView.vue`

**Зміни:**
- [ ] Модалки MFA/WebAuthn/Backup → `<Modal>`
- [ ] Форми логіну/реєстрації: сирі інпути + кнопки → `<FormField>` + `<Button>`
- [ ] Перевірити 3 теми

---

### Хвиля 2 — Основний функціонал (пріоритет 🟡)

#### 5.4. Модуль `marketplace/` (~40 кнопок, 5 модалок)

**Зміни:**
- [ ] Draft/Merge/Publish модалки → `<Modal>`
- [ ] Фільтри → `<Button pill>`
- [ ] Форми профілю тьютора
- [ ] Перевірити 3 теми

---

#### 5.5. Модуль `profile/` (~25 кнопок, 2 модалки)

**Зміни:**
- [ ] TelegramNotifications — вже частково мігрований
- [ ] Settings форми → `<FormField>`
- [ ] Перевірити 3 теми

---

#### 5.6. Модуль `booking/` (~150 кнопок, 7 модалок) ⚠️ Найбільший

**Файли (ключові):**
- `CalendarFooter.vue` — хардкоджені кольори (#1976D2)
- `BookingForm.vue` / `BookingModal.vue`
- `CreateLessonModal.vue`
- `RescheduleModal.vue`
- `SlotEditor.vue`

**Зміни:**
- [ ] CalendarFooter: замінити `#1976D2` → `var(--accent)`
- [ ] Всі модалки → `<Modal>`
- [ ] Кнопки слотів → `<Button>`
- [ ] Форми бронювання → `<FormField>`
- [ ] Перевірити 3 теми

---

#### 5.7. Модуль `payments/` (~10 кнопок, 2 модалки)

**Зміни:**
- [ ] Cancel/Subscription модалки → `<Modal>`
- [ ] Кнопки → `<Button>`
- [ ] Перевірити 3 теми

---

### Хвиля 3 — Допоміжне (пріоритет 🟢)

#### 5.8. Модуль `chat/` (~20 кнопок, 2 модалки)

**Зміни:**
- [ ] ChatModal → `<Modal>`
- [ ] Кнопки введення/відправки → `<Button>`
- [ ] Перевірити 3 теми

---

#### 5.9. Модуль `classroom/` (~15 кнопок, 2 модалки)

**Зміни:**
- [ ] Invite/History модалки → `<Modal>`
- [ ] ClassroomButton.vue → використати `<Button>` з пропсами
- [ ] Перевірити 3 теми

---

#### 5.10. Модуль `winterboard/` (~100 кнопок, 1 модалка)

**Стратегія:** Мігрувати ТІЛЬКИ модалку (Export). Canvas-кнопки (zoom, toolbar) залишити як є — вони специфічні і не впливають на загальний вигляд.

**Зміни:**
- [ ] ExportModal → `<Modal>`
- [ ] Toolbar кнопки — НЕ ЧІПАТИ
- [ ] Перевірити 3 теми

---

#### 5.11. Модуль `staff/` (~30 кнопок, 3 модалки)

**Стратегія:** Останній, бо клієнт не бачить адмінку.

**Зміни:**
- [ ] Grant/Finalize/Report модалки → `<Modal>`
- [ ] Кнопки → `<Button>`
- [ ] Перевірити 3 теми

---

## Правила міграції

1. **1 модуль = 1 коміт** формату `refactor(MODULE): migrate to design system components`
2. **Перед комітом:** `npm run build` ОБОВ'ЯЗКОВО
3. **Перед деплоєм:** перевірити в 3 темах (Light, Dark, Classic)
4. **Якщо щось зламалось:** відкат одного коміту (`git revert HEAD`)
5. **Не змінювати логіку** — тільки template і styles, script залишаємо

---

## Перевірка (за кожний модуль)

- [ ] `npm run build` — OK
- [ ] Всі кнопки використовують `<Button>`
- [ ] Всі модалки використовують `<Modal>`
- [ ] Фокус працює в модалках
- [ ] Escape закриває модалки
- [ ] Light / Dark / Classic — OK
- [ ] Mobile (375px) — OK
