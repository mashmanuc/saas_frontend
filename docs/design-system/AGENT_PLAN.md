# Design System — План для 3 агентів

> Мета: паралельна робота 3 агентів для прискорення Design System Stabilization
> Оцінка: 13-20 днів (1 агент) → **5-7 днів (3 агенти)**

---

## Агенти

| Агент | Зона відповідальності | Файли |
|-------|----------------------|-------|
| **A — Tokens & CSS** | Токени, CSS-класи, tailwind, теми, очищення | `src/styles/`, `src/assets/main.css`, `tailwind.config.js` |
| **B — Components** | UI-компоненти: Button, Textarea, FormField, Modal, ConfirmModal, Input, Select | `src/ui/` |
| **C — Migration** | Міграція модулів на нові компоненти, хардкоджені кольори | `src/modules/` |

---

## Правила

1. **Зони не перетинаються.** Кожен агент працює тільки у своїй зоні файлів
2. **Naming:** Коміти: `design(AGENT-TASK): опис` (наприклад `design(A-1.1): create tokens.css`)
3. **Блокери:** Якщо агент заблокований — працює над наступною незалежною задачею
4. **Тестування:** Після кожної задачі: `npm run build` + перевірка 3 тем
5. **i18n:** Весь текст через `t()`, нові ключі додавати в `uk.json` + `en.json`
6. **Backward compatible:** Старе НЕ видаляти до Фази 6

---

## Мегафаза 0 — Фундамент (День 1)

> Блокер для всього. Виконує **Агент A** соло. Агенти B і C чекають.

| # | Задача | Агент | Залежність | Файли |
|---|--------|-------|------------|-------|
| 0.1 | Створити `src/styles/tokens.css` | A | — | `src/styles/tokens.css` |
| 0.2 | Підключити в `main.js` першим імпортом | A | 0.1 | `src/main.js` |
| 0.3 | Позначити старі файли `@deprecated` | A | 0.1 | `src/ui/tokens.css`, `src/assets2/ui-contract/tokens/tokens.css` |
| 0.4 | Синхронізувати `tailwind.config.js` | A | 0.1 | `tailwind.config.js` |
| ✓ | **Checkpoint:** `npm run build` OK, токени доступні глобально | A | 0.1-0.4 | — |

**DoD Мегафази 0:**
- `--radius-md`, `--space-md`, `--z-modal` доступні в DevTools
- Build проходить
- 3 теми не зламані

---

## Мегафаза 1 — Компоненти + CSS (Дні 2-3)

> Після Мегафази 0 — всі 3 агенти працюють паралельно.

### Агент A — CSS-класи для форм (Фаза 4)

| # | Задача | Залежність | Файли |
|---|--------|------------|-------|
| A-1.1 | Додати `.form-stack`, `.form-row`, `.form-actions` в `main.css` | MF0 | `src/assets/main.css` |
| A-1.2 | Уніфікувати `.input` / `.form-control` на токени | MF0 | `src/assets/main.css` |
| A-1.3 | Додати `.form-label`, `.form-error`, `.form-hint` | MF0 | `src/assets/main.css` |
| A-1.4 | Уніфікувати `.form-group` глобально | MF0 | `src/assets/main.css` |
| ✓ | **Checkpoint:** build OK, `.form-control` = `.input` стилі | — | — |

### Агент B — UI-компоненти (Фази 2 + 3)

| # | Задача | Залежність | Файли |
|---|--------|------------|-------|
| B-1.1 | Оновити `Button.vue` (pill, iconOnly, fullWidth) | MF0 | `src/ui/Button.vue` |
| B-1.2 | Створити `Textarea.vue` | MF0 | `src/ui/Textarea.vue` |
| B-1.3 | Створити `FormField.vue` | MF0 | `src/ui/FormField.vue` |
| B-1.4 | Оновити `Input.vue` на токени | MF0 | `src/ui/Input.vue` |
| B-1.5 | Оновити `Select.vue` на токени | MF0 | `src/ui/Select.vue` |
| B-2.1 | Створити `Modal.vue` (teleport, focus trap, a11y) | B-1.1 | `src/ui/Modal.vue` |
| B-2.2 | Створити `ConfirmModal.vue` | B-2.1 | `src/ui/ConfirmModal.vue` |
| ✓ | **Checkpoint:** build OK, всі компоненти працюють у 3 темах | — | — |

### Агент C — Підготовка до міграції

> Поки B і A роблять компоненти, C робить аудит і готує план міграції.

| # | Задача | Залежність | Файли |
|---|--------|------------|-------|
| C-1.1 | Аудит: скільки сирих `<button>` в кожному модулі | — | — |
| C-1.2 | Аудит: скільки кастомних overlay-модалок | — | — |
| C-1.3 | Аудит: скільки хардкоджених кольорів | — | — |
| C-1.4 | Створити чеклист міграції для кожного модуля | — | `docs/design-system/migration-checklist.md` |
| C-1.5 | Почати міграцію Фаза 4-only модулів (CSS-класи без компонентів) | A-1.* | `src/modules/` |

---

## Мегафаза 2 — Міграція модулів (Дні 3-6)

> Всі 3 агенти мігрують модулі паралельно. Кожен агент бере свою групу.
> **Залежність:** Мегафаза 1 завершена (компоненти + CSS готові).

### Розподіл модулів між агентами

| Агент | Модулі | Кнопок | Модалок | Обсяг |
|-------|--------|--------|---------|-------|
| **A** | `auth/` (30), `dashboard/` (20), `payments/` (10), `staff/` (30) | ~90 | 9 | Середній |
| **B** | `inquiries/` (15), `marketplace/` (40), `profile/` (25), `chat/` (20) | ~100 | 12 | Середній |
| **C** | `booking/` (150), `classroom/` (15), `winterboard/` (100) | ~265 | 10 | Великий (booking!) |

> Booking — найбільший модуль, тому C отримує менше модулів за кількістю.

### Агент A — Група 1

| # | Модуль | Кнопок | Модалок | Пріоритет |
|---|--------|--------|---------|-----------|
| A-2.1 | `auth/` | ~30 | 4 (MFA, WebAuthn, Backup, Unlock) | 🔴 |
| A-2.2 | `dashboard/` | ~20 | 0 | 🔴 |
| A-2.3 | `payments/` | ~10 | 2 | 🟡 |
| A-2.4 | `staff/` | ~30 | 3 | 🟢 |

### Агент B — Група 2

| # | Модуль | Кнопок | Модалок | Пріоритет |
|---|--------|--------|---------|-----------|
| B-3.1 | `inquiries/` | ~15 | 3 | 🔴 |
| B-3.2 | `marketplace/` | ~40 | 5 | 🟡 |
| B-3.3 | `profile/` | ~25 | 2 | 🟡 |
| B-3.4 | `chat/` | ~20 | 2 | 🟢 |

### Агент C — Група 3

| # | Модуль | Кнопок | Модалок | Пріоритет |
|---|--------|--------|---------|-----------|
| C-2.1 | `booking/` | ~150 | 7 | 🟡 ⚠️ Найбільший |
| C-2.2 | `classroom/` | ~15 | 2 | 🟢 |
| C-2.3 | `winterboard/` | ~100 | 1 (тільки Export) | 🟢 (toolbar НЕ чіпати) |

### Правила міграції

1. **1 модуль = 1 коміт:** `design(X-N.N): migrate MODULE to design system`
2. **Перед комітом:** `npm run build` ОБОВ'ЯЗКОВО
3. **Не змінювати логіку** — тільки template і styles
4. **Кроки для кожного модуля:**
   - Замінити сирі `<button class="btn">` → `<Button variant="...">`
   - Замінити сирі `<textarea>` → `<Textarea>`
   - Замінити кастомні overlay → `<Modal>`
   - Замінити `.form-group` → `<FormField>` або залишити з глобальним CSS
   - Замінити хардкоджені кольори → CSS-змінні
   - Перевірити 3 теми

---

## Мегафаза 2.5 — Добивання хвостів (День 3)

> Аудит після MF2 показав: 15 модулів НЕ були покриті + великі залишки в покритих.
> Стан: ~1035 сирих `<button>`, 48 `<textarea>`, 7 overlay, 2580 hex.
> Промти: `PROMPTS_MF2_TAILS.md`

### Розподіл

| Агент | Модулі | ~Buttons | Тип |
|-------|--------|----------|-----|
| **A** | reviews, billing, lessons, onboarding, entitlements, diagnostics | ~122 | Нові |
| **B** | matches, people, trust, contacts, negotiation, call, classrooms, profileV2, student, tutor, tutors, admin | ~96 | Нові (дрібні) |
| **C** | booking↻, marketplace↻, classroom↻, board, profile↻, chat↻, інші↻ | ~500+ | Залишки |

### Виключення (свідомо НЕ мігрувати)

- winterboard/ toolbar, canvas, color picker — canvas UI
- classroom/ board toolbars — canvas UI
- dev/ — тестовий модуль
- chat inline buttons — chat UI
- booking/debug/ — dev-only
- Brand hex (#229ED9, #1DB954)

---

## Мегафаза 3 — Очищення + QA (Дні 4-5)

> Після завершення міграції всіх модулів.

### Агент A — Очищення CSS (Фаза 6)

| # | Задача | Файли |
|---|--------|-------|
| A-3.1 | Консолідація тем-систем (themeStore.js vs .ts) | `src/stores/` |
| A-3.2 | Видалити `src/ui/tokens.css` (після grep) | `src/ui/tokens.css` |
| A-3.3 | Видалити `src/assets2/ui-contract/tokens/tokens.css` (після grep) | `src/assets2/` |
| A-3.4 | Очистити `main.css` від старих `.btn` | `src/assets/main.css` |
| A-3.5 | Фінальний аудит хардкоджених кольорів | `src/modules/` |

### Агент B — Очищення компонентів (Фаза 6)

| # | Задача | Файли |
|---|--------|-------|
| B-4.1 | Видалити `components/ui/Modal.vue` (після grep) | `src/components/ui/` |
| B-4.2 | Видалити `ConfirmDialog.vue` (після grep) | `src/components/ui/` |
| B-4.3 | Перенести потрібне з `m4sh.css` → `tokens.css`/`main.css` | `src/styles/` |

### Агент C — QA (Фаза 7)

| # | Перевірка | Light | Dark | Classic |
|---|-----------|-------|------|---------|
| C-3.1 | Кнопки (всі варіанти) | ⬜ | ⬜ | ⬜ |
| C-3.2 | Модалки (backdrop, focus trap, Esc) | ⬜ | ⬜ | ⬜ |
| C-3.3 | Форми (focus, error, disabled) | ⬜ | ⬜ | ⬜ |
| C-3.4 | Картки (border, shadow) | ⬜ | ⬜ | ⬜ |
| C-3.5 | Mobile (375px) | ⬜ | ⬜ | ⬜ |
| C-3.6 | Tablet (768px) | ⬜ | ⬜ | ⬜ |
| C-3.7 | Desktop (1280px) | ⬜ | ⬜ | ⬜ |
| C-3.8 | Accessibility (focus trap, Esc, contrast) | ⬜ | ⬜ | ⬜ |
| C-3.9 | Фінальний аудит метрик | — | — | — |

---

## Таймлайн

```
День 1:  MF0 ─── A соло (токени, tailwind)                                          ✅
         │
День 1:  MF1 ─── A (CSS форм) ║ B (компоненти) ║ C (аудит + підготовка)             ✅
         │
День 1:  MF2 ─── A (auth,dash,pay,staff) ║ B (inq,mkt,profile,chat) ║ C (booking)   ✅
         │
День 2:  MF2.5── A (reviews,billing,lessons,onb) ║ B (12 дрібних) ║ C (залишки)     ✅
         │
День 2:  MF3 ─── A (очищення CSS) ║ B (btn→Button 106 файлів) ║ C (QA всіх тем)    ✅
         │
День 2:  MF4 ─── A (themeStore консолідація) ║ B (3 btn + 1 overlay) ║ C (QA модалок)  ✅
         │
День 2:  MF5 ─── A (Button.vue self-contained → booking+classroom) ║ B (mkt+operator+misc) ║ C (QA)  ✅
         │
День 2:  MF6 ─── A (залишки booking+mkt) ║ B (ErrorBoundary+PushHandler+RelationAction) ║ C (QA)  ✅
         │
День 3:  MF7 ─── A (booking+mkt bg:white→vars 14 файлів) ║ B (components+views 16 файлів) ║ C (3 файли + QA)
```

**Факт: MF0–MF6 завершені за 2 дні!**

---

## Метрики успіху

| Метрика | До | Після MF6 | Ціль MF7 | Перевірка |
|---------|-----|-----------|----------|-----------|
| `.btn-primary` scoped (не Button.vue) | 27 | 0 ✅ | 0 | `grep -rl '\.btn-primary' src/` |
| `variant="destructive"` | 2 | 0 ✅ | 0 | `grep -rl 'destructive' src/` |
| `background: white` (user-facing) | 55 | 34 | 0 | `grep -rl 'background:\s*white' src/` |
| Button.vue self-contained | ні | так ✅ | так | Стилі всередині компонента |
| Build | OK | OK | OK | `npm run build` |

---

## Критичні правила для агентів

1. **НЕ чіпати чужу зону файлів** — конфлікти merge
2. **Кожен коміт — build OK** — зламаний build блокує всіх
3. **Backward compatible** — старе не видаляти до MF3
4. **1 модуль = 1 коміт** — легкий відкат
5. **Не змінювати логіку** — тільки template/styles
6. **i18n** — весь текст через `t()`, нові ключі в обидва locale файли
7. **Winterboard toolbar — НЕ ЧІПАТИ** — специфічний canvas UI
