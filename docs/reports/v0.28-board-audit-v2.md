# Повний аудит Konva Board v0.28.2

> **Дата:** 2025-12-14  
> **Статус:** In Progress  
> **Автор:** Frontend Team

---

## 🔴 Критичні проблеми (виявлені користувачем)

### 1. Кола (Circle) не вставляються

**Симптом:** Інструмент "Circle" є на toolbar, але при малюванні нічого не відбувається.

**Причина:** В `BoardCanvas.vue`:
- Toolbar має `{ id: 'circle', icon: IconCircle, shortcut: 'C' }`
- Але в template немає рендерингу для `stroke.tool === 'circle'`
- В `handleMouseMove` і `handleMouseUp` немає обробки для `circle`
- Stroke interface не включає `'circle'` як tool type

**Файли для зміни:**
- `src/modules/classroom/components/board/BoardCanvas.vue`

**Рішення:**
1. Додати `'circle'` до Stroke interface
2. Додати `<v-ellipse>` або `<v-circle>` в template
3. Додати `getCircleConfig()` функцію
4. Додати обробку в `handleMouseMove` і `handleMouseUp`
5. Додати preview для circle

---

### 2. Видалення картинки з дошки неможливе

**Симптом:** Немає способу видалити вставлену картинку (asset).

**Причина:** 
- Немає UI для видалення вибраного asset
- Немає `deleteAsset` action в boardStore
- Delete key handler не обробляє assets

**Файли для зміни:**
- `src/modules/classroom/board/state/boardStore.ts`
- `src/modules/classroom/components/board/BoardCanvas.vue`
- `src/modules/solo/views/SoloWorkspace.vue`

**Рішення:**
1. Додати `deleteAsset(assetId)` action в boardStore
2. Обробляти Delete/Backspace для вибраних assets
3. Додати контекстне меню або кнопку видалення

---

### 3. Картинки рендеряться поверх strokes (z-index)

**Симптом:** При вставці картинки, strokes опиняються під нею.

**Причина:** В template порядок layers:
```vue
<v-layer>Background</v-layer>
<v-layer ref="strokesLayerRef">Strokes</v-layer>  <!-- Нижче -->
<v-layer ref="assetsLayerRef">Assets</v-layer>    <!-- Вище -->
```

**Рішення:**
1. Поміняти порядок layers (assets під strokes)
2. АБО додати z-index управління для кожного елемента
3. АБО об'єднати strokes і assets в один layer з сортуванням по zIndex

---

### 4. Експорт дає помилки

**Симптом:** При експорті в PNG/PDF/JSON — "Export failed", 404 помилка.

**Причина:** 
- Backend endpoint `/v1/exports/{exportId}/` не існує
- `soloApi.getExportStatus()` викликає неіснуючий endpoint

**Поточний код:**
```typescript
getExportStatus: (exportId: string): Promise<ExportRequest> =>
  apiClient.get(`/v1/exports/${exportId}/`),  // 404!
```

**Рішення (Frontend):**
1. Тимчасово: Реалізувати client-side export (Konva toDataURL)
2. Постійно: Чекати backend реалізацію

**Рішення (Backend):**
Див. `docs/backend-tasks/v0.28-export-api.md`

---

### 5. Нові сторінки копіюють вміст

**Симптом:** При створенні нової сторінки, вміст першої сторінки копіюється.

**Причина:** В `boardStore.addPage()`:
```typescript
addPage(): void {
  const newPage: Page = {
    id: `page-${Date.now()}`,
    name: `Page ${this.pages.length + 1}`,
    strokes: [],  // Порожній масив
    assets: [],
  }
  this.pages.push(newPage)
  this.currentPageIndex = this.pages.length - 1
  // ...
}
```

Код виглядає правильним. Проблема може бути в:
1. `BoardDock.currentStrokes` computed не оновлюється при зміні `currentPageIndex`
2. Reactivity issue з `pages` array

**Діагностика:**
- Перевірити чи `currentPageIndex` змінюється
- Перевірити чи `currentStrokes` computed перераховується

---

### 6. Кнопка "T" (Text) не працює

**Симптом:** При натисканні на "T" і кліку на canvas — текст не створюється.

**Причина:** В `handleMouseDown`:
```typescript
if (currentTool.value === 'text') {
  createTextAtPosition(pos)
  return
}
```

`createTextAtPosition` створює stroke і відкриває textarea, але:
1. `editingText` встановлюється, але textarea може не показуватись
2. `textEditStyle` може мати неправильні координати

**Діагностика:**
- Перевірити `editingText` ref
- Перевірити CSS для `.text-edit-overlay`

---

### 7. Відеочат відкривається автоматично

**Симптом:** При створенні дошки відразу з'являється вікно відеочату.

**Причина:** В `SoloWorkspace.vue`:
```vue
<LocalVideoPreview
  v-if="showVideo"  <!-- showVideo = ref(true) за замовчуванням -->
  ...
/>
```

**Рішення:**
1. Змінити `showVideo = ref(false)` за замовчуванням
2. Додати кнопку для запуску відео

---

## 🟡 Середні проблеми (з попереднього аудиту)

### 8. Моргання при autosave

**Симптом:** Canvas моргає при кожному autosave.

**Причина:** Reactivity triggers full re-render.

**Рішення:**
- Використовувати `shallowRef` для strokes/assets
- Мемоізувати computed properties

---

### 9. Undo/Redo не працює для assets

**Симптом:** Undo не відміняє додавання/видалення картинок.

**Причина:** `undoStack` зберігає тільки strokes.

**Рішення:**
- Зберігати повний snapshot: `{ strokes, assets }`

---

## 🟢 Низькі проблеми

### 10. WebSocket disconnected помилки

**Причина:** Realtime sync намагається підключитися для solo mode.

**Рішення:** Вимкнути realtime для solo sessions.

---

### 11. Eraser не працює для shapes

**Причина:** `handleErase` перевіряє тільки points proximity.

**Рішення:** Додати bounding box detection для shapes.

---

### 12. Highlighter opacity

**Причина:** `globalCompositeOperation: 'multiply'` може не працювати.

**Рішення:** Використовувати просто `opacity: 0.4`.

---

## 📋 План виправлень

### Фаза 1: Критичні (сьогодні)

| # | Проблема | Оцінка | Пріоритет |
|---|----------|--------|-----------|
| 1 | Circle tool | 1h | 🔴 High |
| 2 | Delete asset | 30m | 🔴 High |
| 3 | Z-index layers | 30m | 🔴 High |
| 4 | Export (client-side) | 2h | 🔴 High |
| 5 | Pages copy bug | 1h | 🔴 High |
| 6 | Text tool | 1h | 🔴 High |
| 7 | Video auto-start | 15m | 🔴 High |

### Фаза 2: Середні

| # | Проблема | Оцінка | Пріоритет |
|---|----------|--------|-----------|
| 8 | Моргання | 1h | 🟡 Medium |
| 9 | Undo/Redo assets | 2h | 🟡 Medium |

### Фаза 3: Низькі

| # | Проблема | Оцінка | Пріоритет |
|---|----------|--------|-----------|
| 10 | WebSocket errors | 1h | 🟢 Low |
| 11 | Eraser shapes | 1h | 🟢 Low |
| 12 | Highlighter | 30m | 🟢 Low |

---

## 🔧 Технічні рішення

### 1. Circle Tool Implementation

```typescript
// Stroke interface
interface Stroke {
  tool: 'pen' | 'highlighter' | 'line' | 'rectangle' | 'circle' | 'text' | 'eraser' | 'select'
  // ...
  radius?: number  // для circle
}

// Template
<v-ellipse
  v-else-if="stroke.tool === 'circle'"
  :config="getCircleConfig(stroke)"
  @click="handleStrokeClick(stroke, $event)"
  @dragend="handleStrokeDragEnd(stroke, $event)"
/>

// Config function
function getCircleConfig(stroke: Stroke): Record<string, unknown> {
  if (!stroke.points[0]) return { visible: false }
  const center = stroke.points[0]
  return {
    id: stroke.id,
    name: `stroke-${stroke.id}`,
    x: center.x,
    y: center.y,
    radiusX: stroke.width ? stroke.width / 2 : 0,
    radiusY: stroke.height ? stroke.height / 2 : 0,
    stroke: stroke.color,
    strokeWidth: stroke.size,
    fill: 'transparent',
    opacity: stroke.opacity,
    draggable: currentTool.value === 'select',
  }
}
```

### 2. Delete Asset

```typescript
// boardStore.ts
deleteAsset(assetId: string): void {
  const page = this.pages[this.currentPageIndex]
  if (!page) return
  
  const index = page.assets.findIndex((a: unknown) => (a as { id: string }).id === assetId)
  if (index !== -1) {
    page.assets.splice(index, 1)
    this.markDirty()
  }
}

// BoardCanvas.vue - handleKeydown
if ((e.key === 'Delete' || e.key === 'Backspace') && selectedNode.value) {
  const nodeId = selectedNode.value.id()
  if (nodeId.startsWith('asset-')) {
    emit('asset-delete', nodeId)
  } else if (nodeId.startsWith('stroke-')) {
    emit('stroke-delete', nodeId)
  }
  selectedNode.value = null
}
```

### 3. Z-index Fix

```vue
<!-- Змінити порядок layers -->
<v-layer>Background</v-layer>
<v-layer ref="assetsLayerRef">Assets (images)</v-layer>  <!-- Нижче -->
<v-layer ref="strokesLayerRef">Strokes</v-layer>         <!-- Вище -->
<v-layer ref="uiLayerRef">UI (transformer, preview)</v-layer>
```

### 4. Client-side Export

```typescript
// ExportMenu.vue
async function exportClientSide(): Promise<void> {
  const stage = boardDockRef.value?.getStage()
  if (!stage) return
  
  if (selectedFormat.value === 'png') {
    const dataUrl = stage.toDataURL({ pixelRatio: 2 })
    downloadDataUrl(dataUrl, `board-${Date.now()}.png`)
  } else if (selectedFormat.value === 'json') {
    const json = JSON.stringify(boardStore.serializedState, null, 2)
    const blob = new Blob([json], { type: 'application/json' })
    downloadBlob(blob, `board-${Date.now()}.json`)
  }
  // PDF потребує додаткової бібліотеки (jspdf)
}
```

### 5. Pages Bug Fix

```typescript
// Перевірити reactivity в BoardDock
const currentStrokes = computed<Stroke[]>(() => {
  const state = props.boardState as { 
    pages?: Array<{ strokes?: Stroke[], id?: string }> 
    currentPageIndex?: number
  }
  
  console.log('[BoardDock] currentPageIndex:', state?.currentPageIndex)
  
  if (!state?.pages?.length) return []
  const pageIndex = state.currentPageIndex ?? 0
  const page = state.pages[pageIndex]
  console.log('[BoardDock] page strokes:', page?.strokes?.length)
  return page?.strokes || []
})
```

---

## ✅ Виконані виправлення (попередня сесія)

| Дата | Проблема | Рішення |
|------|----------|---------|
| 2025-12-13 | Strokes не рендерились | Замінив v-line на v-path |
| 2025-12-13 | Tool не переключався | Додав props tool/color/size |
| 2025-12-13 | Pages structure | Перенесено strokes в pages[] |
| 2025-12-13 | Select tool | Додано draggable + handlers |

---

## ✅ Виконані виправлення (ця сесія)

| Дата | Проблема | Рішення |
|------|----------|---------|
| 2025-12-14 | Video auto-start | `showVideo = ref(false)` |
| 2025-12-14 | Z-index layers | Переставлено layers: assets → strokes → UI |
| 2025-12-14 | Circle tool | Додано v-ellipse, getCircleConfig, preview |
| 2025-12-14 | Delete assets | Додано deleteAsset action, handleKeydown Delete |
| 2025-12-14 | Pages copy bug | Виправлено addPage() - тепер викликає boardStore.addPage() |
| 2025-12-14 | Client-side export | Додано JSON export fallback |
| 2025-12-14 | Подвійний paste | Видалено дублюючий @paste з div, залишено тільки document.addEventListener |
| 2025-12-14 | Моргання autosave | **ГЛИБОКЕ ВИПРАВЛЕННЯ**: Змінено передачу props - тепер strokes/assets напряму замість serializedState |
| 2025-12-14 | Asset add/update | Додано addAsset(), updateAsset() в boardStore + обробку в SoloWorkspace |
| 2025-12-14 | PNG export | Додано детальне логування, виправлено getStage?.() |
| 2025-12-14 | PDF export | Реалізовано client-side PDF export через canvas (A4 формат) |

## 🔧 Ключове виправлення моргання

**Причина моргання:** `boardStore.serializedState` передавався як prop до BoardDock. Цей getter створював новий об'єкт кожен раз, коли будь-яка властивість store змінювалась (включаючи `isDirty`, `lastSavedAt`). Vue бачив новий об'єкт і перерендерював весь BoardDock.

**Рішення:**
1. Змінено props BoardDock: замість `board-state` тепер `strokes` і `assets` напряму
2. Оптимізовано `updateSession()` - не змінює `lastSavedAt` якщо різниця < 1 секунди
3. Додано перевірки `if (this.syncError !== null)` перед зміною

## 🚀 Наступні кроки

1. ✅ ~~Виправити Circle tool~~
2. ✅ ~~Додати Delete для assets~~
3. ✅ ~~Виправити z-index layers~~
4. ✅ ~~Реалізувати client-side export (JSON)~~
5. ✅ ~~Дебагнути pages copy bug~~
6. ✅ ~~Вимкнути auto-start video~~
7. ✅ ~~Виправити подвійний paste~~
8. ✅ ~~Виправити моргання autosave~~ (ГЛИБОКЕ ВИПРАВЛЕННЯ)
9. ✅ ~~PNG export - додано доступ до Konva stage~~
10. ✅ ~~PDF export - реалізовано client-side~~
11. ⚠️ Text tool - додано логування для діагностики
