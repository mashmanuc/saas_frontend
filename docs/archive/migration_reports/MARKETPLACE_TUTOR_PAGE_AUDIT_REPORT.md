# Аудит сторінки тьютора на маркетплейсі — Звіт

**Дата:** 2026-02-03  
**URL:** `http://localhost:5173/marketplace/tutors/:slug`  
**Компонент:** `src/modules/marketplace/views/TutorProfileView.vue`

---

## 🔴 КРИТИЧНІ ПРОБЛЕМИ

### 1. ❌ Відсутня видима кнопка "Надіслати запит" для студента

**Проблема:**
- Кнопка "Надіслати запит" (`@inquiry`) існує в `ProfileContact.vue:59-62`
- **АЛЕ:** вона показується тільки якщо `isStudent === true`
- На скріншоті користувача кнопка НЕ ВИДНА → користувач не авторизований або не є студентом

**Код:**
```vue
<!-- ProfileContact.vue:59-62 -->
<button v-if="isStudent" class="btn btn-primary" @click="emit('inquiry')" data-test="inquiry-cta">
  <Send :size="18" />
  {{ t('inquiries.form.title') }}
</button>
```

**Наслідки:**
- Студент не може створити inquiry з маркетплейсу
- Основний CTA (Call To Action) для конверсії відсутній
- Порушення user flow: студент приходить на профіль → не може зв'язатися

**Статус інтеграції:**
- ✅ `InquiryFormModal` вже імпортовано в `TutorProfileView.vue:26`
- ✅ Modal вже підключено в template `TutorProfileView.vue:335-347`
- ✅ Handlers `openInquiryModal`, `closeInquiryModal`, `handleInquirySuccess` вже існують
- ❌ Кнопка прихована за `v-if="isStudent"`

---

### 2. ❌ Кнопка "Надіслати повідомлення" не функціонує

**Проблема:**
```typescript
// TutorProfileView.vue:71-73
function handleMessage() {
  return  // ❌ Порожня функція, нічого не робить
}
```

**Наслідки:**
- Користувач клікає "Надіслати повідомлення" → нічого не відбувається
- Broken UX, втрата довіри користувача
- Кнопка займає місце, але не працює

**Рекомендація:**
- Або реалізувати функціонал (чат/месенджер)
- Або приховати кнопку до реалізації
- Або показувати tooltip "Скоро буде доступно"

---

### 3. ⚠️ Неповна інформація про тьютора в ProfileHeader

**Проблема:**
```typescript
// ProfileHeader.vue:25-28
const fullName = computed(() => {
  // TutorProfileFull doesn't have user.full_name, use slug as fallback
  return props.profile?.slug || t('common.notSpecified')
})
```

**Що не так:**
- Замість імені тьютора показується `slug` (наприклад, "cergio-lolop")
- `countryText` завжди показує "Не вказано" (hardcoded)
- Відсутня реальна інформація про країну

**Наслідки:**
- Студент не бачить справжнє ім'я тьютора
- Відсутня географічна інформація (важлива для часових поясів)
- Виглядає незавершено та непрофесійно

---

## 🟠 СЕРЕДНІ ПРОБЛЕМИ

### 4. ⚠️ Відсутня інформація про мови викладання

**Проблема:**
- В `ProfileHeader` немає секції з мовами
- `TutorProfileFull` має поле `languages`, але воно не відображається
- Студент не знає, якими мовами викладає тьютор

**Рекомендація:**
- Додати секцію "Мови викладання" в ProfileHeader або ProfileAbout
- Показувати список мов з рівнем володіння

---

### 5. ⚠️ Календар доступності не завжди показується

**Проблема:**
```vue
<!-- TutorProfileView.vue:254-257 -->
<section 
  v-if="currentProfile.user_id && currentProfile.availability_summary?.timezone" 
  class="profile-section" 
>
```

**Що не так:**
- Календар показується тільки якщо є `timezone`
- Якщо тьютор не налаштував доступність → порожня сторінка
- Немає fallback повідомлення "Тьютор ще не налаштував розклад"

---

### 6. ⚠️ Відсутня секція "Про мене" (bio)

**Проблема:**
- `ProfileAbout` показує тільки `bio`
- Якщо `bio` порожнє → секція порожня або відсутня
- Немає fallback тексту

**Рекомендація:**
- Показувати placeholder "Тьютор ще не додав опис"
- Або приховувати секцію, якщо вона порожня

---

### 7. ⚠️ Відсутня інформація про досвід роботи

**Проблема:**
- `ProfileHeader` показує `experience_years`, але це тільки число
- Немає деталей: де працював, які досягнення, сертифікати
- `ProfileEducation` показує тільки освіту, але не досвід

**Рекомендація:**
- Додати секцію "Досвід роботи" з деталями
- Показувати попередні місця роботи, проєкти, досягнення

---

## 🟡 ДРІБНІ ПРОБЛЕМИ ТА ПОКРАЩЕННЯ UX

### 8. 🟡 Кнопка "Забронювати урок" скролить до календаря

**Проблема:**
```typescript
// TutorProfileView.vue:64-69
function handleBook() {
  // Booking is now handled via trial-request flow (weekly availability).
  // Keep CTA wired, but require slot selection.
  const el = document.querySelector('[data-test="marketplace-availability"]') as HTMLElement | null
  el?.scrollIntoView?.({ behavior: 'smooth', block: 'start' })
}
```

**Що не так:**
- Кнопка "Забронювати урок" просто скролить до календаря
- Немає прямого бронювання
- Користувач очікує відкриття модального вікна або форми

**Рекомендація:**
- Залишити як є (scroll до календаря)
- Додати tooltip "Оберіть час у календарі нижче"

---

### 9. 🟡 Відсутня інформація про відгуки в header

**Проблема:**
- `ProfileHeader` показує тільки `Rating` (зірки + кількість відгуків)
- Немає короткого опису відгуків (наприклад, "95% студентів рекомендують")
- Немає preview останніх відгуків

**Рекомендація:**
- Додати короткий summary відгуків в header
- Показувати 1-2 останні відгуки як preview

---

### 10. 🟡 Відсутня кнопка "Поділитися профілем"

**Проблема:**
- Немає можливості поділитися профілем тьютора
- Студент не може надіслати лінк друзям

**Рекомендація:**
- Додати кнопку "Поділитися" (Share) в ProfileHeader
- Копіювати лінк у буфер обміну або відкривати native share dialog

---

### 11. 🟡 Відсутня інформація про ціни в header

**Проблема:**
- Ціна показується тільки в sidebar (`ProfileContact`)
- На mobile sidebar внизу → користувач не бачить ціну одразу

**Рекомендація:**
- Додати ціну в `ProfileHeader` (десктоп + mobile)
- Показувати діапазон цін, якщо є різні предмети

---

### 12. 🟡 Відсутня кнопка "Додати в обране"

**Проблема:**
- Немає можливості зберегти тьютора в обране
- Студент не може створити список улюблених тьюторів

**Рекомендація:**
- Додати кнопку "Додати в обране" (серце/зірка) в ProfileHeader
- Зберігати в localStorage або backend

---

### 13. 🟡 Меню "⋮" (три крапки) не інтуїтивне

**Проблема:**
```vue
<!-- TutorProfileView.vue:220-236 -->
<div v-if="auth.isAuthenticated" class="profile-actions-menu">
  <button class="actions-menu-btn">⋮</button>
  <div v-if="showActionsMenu" class="actions-dropdown">
    <button class="menu-item" @click="handleReport">Поскаржитися</button>
    <button class="menu-item" @click="handleBlock">Заблокувати</button>
  </div>
</div>
```

**Що не так:**
- Меню показується тільки авторизованим користувачам
- Містить тільки негативні дії (Report, Block)
- Немає позитивних дій (Share, Save, etc.)

**Рекомендація:**
- Додати позитивні дії в меню
- Або перемістити Report/Block в окреме місце

---

## 📊 ВІЗУАЛЬНІ ПРОБЛЕМИ

### 14. 🎨 Відсутня ієрархія контенту

**Проблема:**
- Всі секції мають однаковий вигляд
- Немає чіткої ієрархії: що важливіше, що другорядне
- Важко швидко знайти потрібну інформацію

**Рекомендація:**
- Виділити найважливіші секції (About, Subjects, Calendar)
- Використовувати різні розміри заголовків
- Додати візуальні акценти (кольори, іконки)

---

### 15. 🎨 Sidebar занадто великий на desktop

**Проблема:**
```css
/* TutorProfileView.vue:368 */
grid-template-columns: 1fr 360px;
```

**Що не так:**
- Sidebar займає 360px (фіксована ширина)
- На великих екранах виглядає незбалансовано
- Багато порожнього простору

**Рекомендація:**
- Зменшити ширину sidebar до 320px
- Або зробити адаптивну ширину (min-content)

---

### 16. 🎨 Відсутні skeleton loaders

**Проблема:**
```vue
<!-- TutorProfileView.vue:214 -->
<LoadingSpinner v-if="isLoadingProfile" class="loading" />
```

**Що не так:**
- Показується тільки spinner
- Користувач не бачить структуру сторінки під час завантаження
- Виглядає як "пуста" сторінка

**Рекомендація:**
- Додати skeleton loaders для header, sidebar, sections
- Показувати структуру сторінки під час завантаження

---

## 🚀 РЕКОМЕНДАЦІЇ ДЛЯ ПОКРАЩЕННЯ

### Пріоритет 1 (P0 — Критичні)

1. **Показувати кнопку "Надіслати запит" для всіх студентів**
   - Прибрати `v-if="isStudent"` або змінити логіку
   - Якщо не авторизований → показувати "Увійдіть, щоб надіслати запит"

2. **Реалізувати або приховати кнопку "Надіслати повідомлення"**
   - Або реалізувати чат
   - Або приховати кнопку до реалізації

3. **Показувати справжнє ім'я тьютора замість slug**
   - Додати `full_name` в `TutorProfileFull` type
   - Оновити backend API для повернення `full_name`

### Пріоритет 2 (P1 — Важливі)

4. **Додати інформацію про мови викладання**
   - Показувати список мов з рівнем володіння

5. **Додати fallback для порожніх секцій**
   - "Тьютор ще не додав опис"
   - "Тьютор ще не налаштував розклад"

6. **Додати секцію "Досвід роботи"**
   - Показувати попередні місця роботи, проєкти

### Пріоритет 3 (P2 — Покращення UX)

7. **Додати кнопку "Поділитися профілем"**
8. **Додати кнопку "Додати в обране"**
9. **Показувати ціну в ProfileHeader (mobile)**
10. **Додати skeleton loaders**
11. **Покращити візуальну ієрархію контенту**

---

## 📝 ТЕХНІЧНЕ ЗАВДАННЯ (на основі аудиту)

### Завдання 1: Виправити кнопку "Надіслати запит"

**Файли:**
- `src/modules/marketplace/components/profile/ProfileContact.vue`

**Зміни:**
```vue
<!-- Замість -->
<button v-if="isStudent" class="btn btn-primary" @click="emit('inquiry')">

<!-- Зробити -->
<button v-if="auth.isAuthenticated" class="btn btn-primary" @click="emit('inquiry')">
  <Send :size="18" />
  {{ t('inquiries.form.title') }}
</button>
<button v-else class="btn btn-primary" @click="emit('login-required')">
  <Send :size="18" />
  {{ t('inquiries.form.loginRequired') }}
</button>
```

**i18n ключі:**
```json
{
  "inquiries": {
    "form": {
      "loginRequired": "Увійдіть, щоб надіслати запит"
    }
  }
}
```

---

### Завдання 2: Приховати або реалізувати кнопку "Надіслати повідомлення"

**Варіант А (приховати):**
```vue
<!-- ProfileContact.vue -->
<button v-if="false" class="btn btn-secondary" @click="emit('message')">
  <MessageCircle :size="18" />
  {{ t('marketplace.profile.contact.sendMessage') }}
</button>
```

**Варіант Б (показувати tooltip):**
```vue
<button class="btn btn-secondary" disabled :title="t('marketplace.profile.contact.messagingComingSoon')">
  <MessageCircle :size="18" />
  {{ t('marketplace.profile.contact.sendMessage') }}
</button>
```

---

### Завдання 3: Показувати справжнє ім'я тьютора

**Backend:**
- Додати `full_name` в `TutorProfileFull` serializer
- Повертати `user.full_name` з API

**Frontend:**
```typescript
// ProfileHeader.vue
const fullName = computed(() => {
  return props.profile?.full_name || props.profile?.slug || t('common.notSpecified')
})
```

---

### Завдання 4: Додати секцію "Мови викладання"

**Новий компонент:**
- `src/modules/marketplace/components/profile/ProfileLanguages.vue`

**Інтеграція:**
```vue
<!-- TutorProfileView.vue -->
<ProfileLanguages
  v-if="Array.isArray(currentProfile.languages) && currentProfile.languages.length > 0"
  :languages="currentProfile.languages"
/>
```

---

## 📸 ВІЗУАЛЬНІ MOCKUP (рекомендації)

### Desktop Layout (рекомендований)

```
┌─────────────────────────────────────────────────────────────┐
│  [← Назад]                                          [⋮ Меню] │
│                                                                │
│  ┌────────┐  Ім'я Тьютора                    ⭐ 4.8 (24)    │
│  │ Avatar │  Headline текст                                   │
│  │        │  📍 Україна | 🕐 UTC+2 | ⭐ 5 років досвіду      │
│  └────────┘  🗣️ Українська, Англійська                       │
│                                                                │
│  ┌─────────────────────────┐  ┌──────────────────────────┐  │
│  │                         │  │  💰 500 грн/год          │  │
│  │  Про мене               │  │  🎓 Пробний урок: 300 грн│  │
│  │  ...                    │  │  🕐 Відповідає за 2 год  │  │
│  │                         │  │                          │  │
│  │  Освіта                 │  │  [📤 Надіслати запит]   │  │
│  │  ...                    │  │  [📅 Забронювати урок]  │  │
│  │                         │  │  [💬 Написати (скоро)]  │  │
│  │  Предмети               │  │                          │  │
│  │  ...                    │  │  [⭐ Додати в обране]   │  │
│  │                         │  │  [🔗 Поділитися]        │  │
│  │  📅 Календар            │  └──────────────────────────┘  │
│  │  [Календар тут]         │                                │
│  │                         │                                │
│  │  ⭐ Відгуки             │                                │
│  │  ...                    │                                │
│  └─────────────────────────┘                                │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ ACCEPTANCE CRITERIA

### Мінімальні вимоги (Must Have):

1. ✅ Кнопка "Надіслати запит" видима для всіх авторизованих користувачів
2. ✅ Кнопка "Надіслати запит" відкриває `InquiryFormModal`
3. ✅ Кнопка "Надіслати повідомлення" прихована або має tooltip "Скоро буде доступно"
4. ✅ Показується справжнє ім'я тьютора (не slug)
5. ✅ Fallback текст для порожніх секцій

### Бажані покращення (Should Have):

6. ✅ Секція "Мови викладання"
7. ✅ Кнопка "Поділитися профілем"
8. ✅ Кнопка "Додати в обране"
9. ✅ Skeleton loaders під час завантаження

### Опціональні покращення (Nice to Have):

10. ✅ Секція "Досвід роботи"
11. ✅ Preview відгуків в header
12. ✅ Покращена візуальна ієрархія

---

**Звіт підготовлено:** 2026-02-03 10:30 UTC+02:00  
**Статус:** ✅ Готово до реалізації
