# 🔍 SOLO v2 INTEGRATION AUDIT & MIGRATION PLAN

> **Дата аудиту:** 2026-02-03  
> **Мета:** Визначити готовність Solo v2 до інтеграції та створити покроковий план міграції з можливістю rollback

---

## 📊 EXECUTIVE SUMMARY

### Статус Solo v2
- ✅ **Код існує:** `docs/SOLO_v2/solo_FE` (53 файли)
- ✅ **Документація:** Повна (API contracts, CHANGELOG, MIGRATION, PROGRESS_LOG)
- ❌ **Інтеграція:** НЕ виконана (код не перенесено в `src/modules/solo`)
- ⚠️ **Drop-in replacement:** **НЕМОЖЛИВИЙ** без адаптації батьківських компонентів

### Висновок
Solo v2 є **робочим прототипом**, але потребує значної адаптації для інтеграції. Пряма заміна файлів неможлива через:
1. Зміни в інтерфейсах компонентів (нові props, events)
2. Відсутність нових composables у поточній версії
3. Інтеграція з classroom/board модулями (SoloWorkspace використовує BoardDock)
4. Відсутність залежності `pdfjs-dist`

---

## 🔬 ДЕТАЛЬНИЙ АУДИТ

### 1. Структура файлів

#### Solo v2 (`docs/SOLO_v2/solo_FE`) - 53 файли
```
solo_FE/
├── api/soloApi.ts                    ✅ Ідентичний контракт
├── components/
│   ├── canvas/SoloCanvas.vue         ⚠️ ПОВНІСТЮ ПЕРЕПИСАНИЙ
│   ├── toolbar/
│   │   ├── SoloToolbar.vue           ⚠️ ПОВНІСТЮ ПЕРЕПИСАНИЙ
│   │   ├── BackgroundPicker.vue      ➕ НОВИЙ
│   │   ├── PdfImportButton.vue       ➕ НОВИЙ
│   │   ├── ColorPicker.vue           ⚠️ Оновлений (dropdown)
│   │   ├── SizePicker.vue            ⚠️ Оновлений (dropdown)
│   │   └── ToolButton.vue            ⚠️ Оновлений (SVG slots)
│   └── [інші компоненти]             ✅ Без змін
├── composables/
│   ├── useKeyboardShortcuts.ts       ➕ НОВИЙ
│   ├── useSelection.ts               ➕ НОВИЙ
│   ├── useHistory.ts                 ➕ НОВИЙ
│   ├── useAutosave.ts                ➕ НОВИЙ
│   ├── useCanvasOptimization.ts      ➕ НОВИЙ
│   ├── usePdfImport.ts               ➕ НОВИЙ
│   └── [старі composables]           ✅ Без змін
├── store/soloStore.ts                ⚠️ Додано autosave state + debounce
├── types/solo.ts                     ⚠️ Додано: ArrowStyle, BackgroundType, PageBackground
└── tests/e2e/new-tools.spec.ts       ➕ НОВИЙ
```

#### Поточна версія (`src/modules/solo`) - 40 файлів
```
solo/
├── api/soloApi.ts                    ✅ Ідентичний
├── components/
│   ├── canvas/SoloCanvas.vue         ❌ СТАРА ВЕРСІЯ (без circle, arrow, selection, background)
│   ├── toolbar/
│   │   ├── SoloToolbar.vue           ❌ СТАРА ВЕРСІЯ (emoji, без групування)
│   │   ├── ColorPicker.vue           ❌ СТАРА ВЕРСІЯ (inline)
│   │   ├── SizePicker.vue            ❌ СТАРА ВЕРСІЯ (inline)
│   │   └── ToolButton.vue            ❌ СТАРА ВЕРСІЯ (без SVG slots)
│   └── [інші компоненти]             ✅ Без змін
├── composables/
│   ├── useCanvas.ts                  ✅ Існує
│   ├── usePages.ts                   ✅ Існує
│   ├── useTools.ts                   ✅ Існує
│   ├── useStorage.ts                 ✅ Існує
│   ├── useCloudStorage.ts            ✅ Існує
│   ├── usePdfExport.ts               ✅ Існує
│   ├── useSharing.ts                 ✅ Існує
│   ├── useConflict.ts                ✅ Існує
│   └── useLocalMedia.ts              ✅ Існує
│   ❌ useKeyboardShortcuts.ts        ВІДСУТНІЙ
│   ❌ useSelection.ts                ВІДСУТНІЙ
│   ❌ useHistory.ts                  ВІДСУТНІЙ
│   ❌ useAutosave.ts                 ВІДСУТНІЙ
│   ❌ useCanvasOptimization.ts       ВІДСУТНІЙ
│   ❌ usePdfImport.ts                ВІДСУТНІЙ
├── store/soloStore.ts                ❌ СТАРА ВЕРСІЯ (без autosave)
├── types/solo.ts                     ❌ СТАРА ВЕРСІЯ (без arrow, background типів)
└── views/SoloWorkspace.vue           ⚠️ ІНТЕГРОВАНИЙ З CLASSROOM (BoardDock, BoardToolbarVertical)
```

---

### 2. Порівняння ключових компонентів

#### SoloCanvas.vue

| Аспект | Поточна версія | Solo v2 | Сумісність |
|--------|---------------|---------|------------|
| **Props** | `page, tool, color, size, zoom, panX, panY` | `page, tool, color, size, zoom, panX, panY, arrowStyle?, arrowSize?, background?` | ⚠️ Нові optional props |
| **Events** | `stroke-end, shape-end, text-create, zoom-change, pan-change` | `stroke-end, shape-end, text-create, items-update, items-delete, tool-change, undo, redo, delete, copy, paste, select-all, escape` | ❌ Нові events обов'язкові |
| **Інструменти** | pen, highlighter, eraser, line, rectangle, text, note, select | + arrow, circle | ⚠️ Нові інструменти |
| **Background** | Тільки білий | white, grid, dots, ruled, graph, color | ❌ Нова функціональність |
| **Selection** | Немає | Rectangle, Lasso, Move, Resize (8 handles) | ❌ Нова функціональність |
| **Composables** | Немає | useKeyboardShortcuts, useSelection | ❌ Залежність від нових composables |

**Висновок:** Drop-in replacement **НЕМОЖЛИВИЙ** - батьківський компонент має обробляти нові events.

#### SoloToolbar.vue

| Аспект | Поточна версія | Solo v2 | Сумісність |
|--------|---------------|---------|------------|
| **Props** | `currentTool, currentColor, currentSize, presetColors, presetSizes` | + `currentArrowStyle?, currentArrowSize?, currentBackground?, isMobile?` | ⚠️ Нові optional props |
| **Events** | `tool-change, color-change, size-change, undo, redo, clear` | + `arrow-style-change, arrow-size-change, background-change` | ⚠️ Нові optional events |
| **UI** | Emoji іконки, inline pickers | SVG іконки, dropdown pickers, групування | ❌ Візуальні зміни |
| **Responsive** | Ні | Так (vertical/horizontal) | ➕ Покращення |

**Висновок:** Частково сумісний, але батьківський компонент має передавати нові props.

#### soloStore.ts

| Аспект | Поточна версія | Solo v2 | Сумісність |
|--------|---------------|---------|------------|
| **State** | `sessions, currentSession, isLoading, error` | + `autosave: {isSaving, lastSaved, pendingChanges, saveCount}, autosaveEnabled` | ⚠️ Нові поля |
| **Actions** | CRUD операції | + `scheduleAutosave(), saveNow(), setAutosaveEnabled()` | ⚠️ Нові методи |
| **Getters** | `getSessionById, recentSessions` | + `isSaving, hasPendingChanges, lastSavedAt, saveCount` | ⚠️ Нові getters |

**Висновок:** Backward compatible (нові поля optional), але батьківський код не використовує autosave.

#### types/solo.ts

| Аспект | Поточна версія | Solo v2 | Сумісність |
|--------|---------------|---------|------------|
| **Tool** | 10 типів | + 'arrow' | ⚠️ Розширення union |
| **Shape.type** | 'line', 'rectangle', 'circle' | + 'arrow' | ⚠️ Розширення union |
| **Shape** | Базові поля | + `radius?, arrowStart?, arrowEnd?, arrowSize?` | ✅ Optional поля (backward compatible) |
| **PageState** | Базові поля | + `background?: PageBackground` | ✅ Optional поле (backward compatible) |
| **Нові типи** | - | `ArrowStyle, BackgroundType, PageBackground` | ➕ Додаткові типи |

**Висновок:** Backward compatible на рівні типів, але старий код не розуміє нові інструменти.

---

### 3. Залежності

#### Поточні залежності (package.json)
```json
{
  "konva": "^10.0.12",           ✅ Є
  "vue-konva": "^3.2.6",         ✅ Є
  "perfect-freehand": "^1.2.2",  ✅ Є
  "pdfjs-dist": ???              ❌ ВІДСУТНЯ
}
```

#### Необхідні для Solo v2
- `pdfjs-dist` (для PDF import) - **ВІДСУТНЯ**, потрібно додати
- Всі інші залежності вже є

**Дія:** Додати `pdfjs-dist` в `package.json` (lazy load, ~500KB)

---

### 4. Інтеграція з SoloWorkspace.vue

**Проблема:** Поточний `SoloWorkspace.vue` використовує:
- `BoardDock` (з `@/modules/classroom/components/board/BoardDock.vue`)
- `BoardToolbarVertical` (з `@/modules/classroom/components/board/BoardToolbarVertical.vue`)
- `useBoardStore()` та `useBoardSyncStore()`

**Solo v2 очікує:**
- Прямий `SoloCanvas.vue` + `SoloToolbar.vue`
- `useSoloStore()`

**Висновок:** Потрібна **повна переробка** `SoloWorkspace.vue` або створення окремого view для Solo v2.

---

## ⚠️ КРИТИЧНІ БЛОКЕРИ ДЛЯ DROP-IN REPLACEMENT

### 1. Зміни в інтерфейсах компонентів
- `SoloCanvas.vue` emit'ить нові events: `items-update`, `items-delete`, `tool-change`, `undo`, `redo`, тощо
- Батьківський компонент (`SoloWorkspace.vue`) **НЕ** обробляє ці events
- **Результат:** Runtime помилки, втрата функціональності

### 2. Відсутність composables
- Solo v2 Canvas використовує `useKeyboardShortcuts()`, `useSelection()`
- Ці файли відсутні в `src/modules/solo/composables/`
- **Результат:** Import errors, build fail

### 3. Архітектурна несумісність
- Поточний Solo інтегрований з Classroom (BoardDock, BoardStore)
- Solo v2 - standalone модуль
- **Результат:** Конфлікт архітектур

### 4. Відсутня залежність
- `pdfjs-dist` не встановлена
- **Результат:** PDF import не працює (але не критично, якщо не використовувати)

---

## 📋 ПЛАН МІГРАЦІЇ (3 ФАЗИ)

### ✅ МОЖЛИВІСТЬ ROLLBACK
Кожна фаза виконується в окремій гілці з можливістю повернення до попереднього стану.

---

### 🔵 ФАЗА 1: ПІДГОТОВКА (Низький ризик)
**Мета:** Додати нові файли без зміни існуючих

#### 1.1. Додати залежності
```bash
cd frontend
npm install pdfjs-dist@4.0.379
```

#### 1.2. Скопіювати нові composables
```bash
# Копіювати з solo_FE/composables/ в src/modules/solo/composables/
- useKeyboardShortcuts.ts + useKeyboardShortcuts.spec.ts
- useSelection.ts + useSelection.spec.ts
- useHistory.ts + useHistory.spec.ts
- useAutosave.ts
- useCanvasOptimization.ts
- usePdfImport.ts
```

#### 1.3. Оновити типи (backward compatible)
```bash
# Замінити src/modules/solo/types/solo.ts
# Додає: ArrowStyle, BackgroundType, PageBackground
# Розширює: Tool (+ arrow), Shape.type (+ arrow), Shape (+ radius, arrow*)
# Додає: PageState.background (optional)
```

#### 1.4. Додати нові компоненти
```bash
# Копіювати в src/modules/solo/components/toolbar/
- BackgroundPicker.vue
- PdfImportButton.vue
```

#### 1.5. Запустити тести
```bash
npm run test:unit
npm run typecheck
```

**Rollback:** `git reset --hard` або видалити нові файли

**Критерій успіху:** Білд проходить, старий код працює без змін

---

### 🟡 ФАЗА 2: ОНОВЛЕННЯ CORE КОМПОНЕНТІВ (Середній ризик)
**Мета:** Замінити ключові компоненти з адаптацією батьківських

#### 2.1. Бекап поточних файлів
```bash
# Створити backup гілку
git checkout -b solo-v2-migration
git checkout -b solo-v1-backup
# Скопіювати старі файли в docs/SOLO_v2/solo_v1_backup/
cp -r src/modules/solo/components/canvas docs/SOLO_v2/solo_v1_backup/
cp -r src/modules/solo/components/toolbar docs/SOLO_v2/solo_v1_backup/
cp src/modules/solo/store/soloStore.ts docs/SOLO_v2/solo_v1_backup/
git checkout solo-v2-migration
```

#### 2.2. Оновити soloStore.ts
```bash
# Замінити src/modules/solo/store/soloStore.ts
# Додає autosave state + actions
```

#### 2.3. Оновити SoloToolbar.vue
```bash
# Замінити src/modules/solo/components/toolbar/SoloToolbar.vue
# Замінити ToolButton.vue, ColorPicker.vue, SizePicker.vue
```

#### 2.4. Оновити SoloCanvas.vue
```bash
# Замінити src/modules/solo/components/canvas/SoloCanvas.vue
# КРИТИЧНО: Це найбільша зміна
```

#### 2.5. Адаптувати SoloWorkspace.vue
**Варіант A: Мінімальна адаптація (рекомендовано)**
```vue
<!-- Додати обробники нових events -->
<SoloCanvas
  :page="currentPage"
  :tool="currentTool"
  :color="currentColor"
  :size="currentSize"
  :zoom="zoom"
  :panX="panX"
  :panY="panY"
  :arrow-style="arrowStyle"
  :arrow-size="arrowSize"
  :background="pageBackground"
  @stroke-end="handleStrokeEnd"
  @shape-end="handleShapeEnd"
  @text-create="handleTextCreate"
  @items-update="handleItemsUpdate"      <!-- НОВИЙ -->
  @items-delete="handleItemsDelete"      <!-- НОВИЙ -->
  @tool-change="handleToolChange"        <!-- НОВИЙ -->
  @undo="handleUndo"                     <!-- НОВИЙ -->
  @redo="handleRedo"                     <!-- НОВИЙ -->
/>

<SoloToolbar
  :current-tool="currentTool"
  :current-color="currentColor"
  :current-size="currentSize"
  :current-arrow-style="arrowStyle"      <!-- НОВИЙ -->
  :current-arrow-size="arrowSize"        <!-- НОВИЙ -->
  :current-background="pageBackground"   <!-- НОВИЙ -->
  @tool-change="handleToolChange"
  @color-change="handleColorChange"
  @size-change="handleSizeChange"
  @arrow-style-change="handleArrowStyleChange"  <!-- НОВИЙ -->
  @arrow-size-change="handleArrowSizeChange"    <!-- НОВИЙ -->
  @background-change="handleBackgroundChange"   <!-- НОВИЙ -->
/>
```

**Варіант B: Повна переробка**
- Створити новий `SoloWorkspaceV2.vue` з нуля
- Використовувати `useSoloStore()` замість `useBoardStore()`
- Видалити інтеграцію з Classroom

#### 2.6. Запустити тести
```bash
npm run test:unit
npm run typecheck
npm run dev
# Мануальне тестування всіх інструментів
```

**Rollback:** 
```bash
git checkout solo-v1-backup
# або
cp -r docs/SOLO_v2/solo_v1_backup/* src/modules/solo/
```

**Критерій успіху:** Всі старі інструменти працюють + нові інструменти доступні

---

### 🟢 ФАЗА 3: ТЕСТУВАННЯ ТА ФІНАЛ (Низький ризик)
**Мета:** Валідація, тести, документація

#### 3.1. E2E тести
```bash
# Скопіювати tests/e2e/new-tools.spec.ts
# Запустити
npm run test:e2e
```

#### 3.2. Перевірка продуктивності
```bash
# Тестувати autosave debounce
# Тестувати canvas optimization
# Перевірити memory leaks
```

#### 3.3. Оновити документацію
```bash
# Оновити README.md
# Оновити CHANGELOG.md
# Створити MIGRATION_GUIDE.md для команди
```

#### 3.4. Code review
- Перевірити всі зміни
- Валідувати backward compatibility
- Перевірити i18n ключі

#### 3.5. Merge в main
```bash
git checkout main
git merge solo-v2-migration
```

**Rollback:** `git revert` або `git reset --hard origin/main`

**Критерій успіху:** Всі тести зелені, продакшн готовий

---

## 🎯 АЛЬТЕРНАТИВНИЙ ПІДХІД: ПАРАЛЕЛЬНА ВЕРСІЯ

Якщо drop-in replacement занадто ризикований:

### Створити окремий модуль Solo v2
```
src/modules/
├── solo/           # Стара версія (залишити як є)
└── solo-v2/        # Нова версія (повна копія з solo_FE)
```

### Роути
```typescript
{
  path: '/solo/:id',
  component: SoloWorkspace,      // Стара версія
},
{
  path: '/solo-v2/:id',
  component: SoloWorkspaceV2,    // Нова версія
}
```

### Переваги
- ✅ Нульовий ризик для існуючих користувачів
- ✅ Можливість A/B тестування
- ✅ Поступова міграція

### Недоліки
- ❌ Подвоєння коду
- ❌ Складність підтримки двох версій
- ❌ Потрібна міграція даних між версіями

---

## 📊 ОЦІНКА ТРУДОМІСТКОСТІ

| Фаза | Завдання | Час (години) | Ризик |
|------|----------|--------------|-------|
| **ФАЗА 1** | Підготовка | 4-6 | Низький |
| - | Встановити залежності | 0.5 | - |
| - | Скопіювати composables | 1 | - |
| - | Оновити типи | 1 | - |
| - | Додати нові компоненти | 1 | - |
| - | Тести + typecheck | 1.5 | - |
| **ФАЗА 2** | Оновлення core | 12-16 | Середній |
| - | Бекап + setup | 1 | - |
| - | Оновити store | 2 | - |
| - | Оновити Toolbar | 2 | - |
| - | Оновити Canvas | 4 | Високий |
| - | Адаптувати Workspace | 3-5 | Високий |
| - | Тестування | 2-4 | - |
| **ФАЗА 3** | Фіналізація | 6-8 | Низький |
| - | E2E тести | 2 | - |
| - | Продуктивність | 2 | - |
| - | Документація | 2 | - |
| - | Code review | 2 | - |
| **РАЗОМ** | | **22-30 годин** | |

---

## ✅ РЕКОМЕНДАЦІЇ

### 1. Обрати підхід
**Рекомендація:** ФАЗА 1-2-3 (поступова міграція)

**Обґрунтування:**
- Менше дублювання коду
- Чистіша архітектура
- Backward compatible типи
- Можливість rollback на кожному кроці

### 2. Пріоритети
1. **P0:** Фаза 1 (підготовка) - безпечна, додає можливості
2. **P1:** Фаза 2.1-2.3 (store + toolbar) - середній ризик
3. **P2:** Фаза 2.4-2.5 (canvas + workspace) - високий ризик, потребує ретельного тестування
4. **P3:** Фаза 3 (тести + документація)

### 3. Тестування
- **Обов'язково:** Мануальне тестування після кожної фази
- **Обов'язково:** Unit тести для нових composables
- **Бажано:** E2E тести для нових інструментів
- **Бажано:** Візуальна регресія (screenshot testing)

### 4. Rollback стратегія
- Кожна фаза в окремій гілці
- Бекап старих файлів в `docs/SOLO_v2/solo_v1_backup/`
- Git tags для кожної фази
- Можливість швидкого повернення через `git revert`

---

## 🚨 КРИТИЧНІ РИЗИКИ

### 1. Інтеграція з Classroom
**Ризик:** SoloWorkspace.vue використовує BoardDock з classroom модуля

**Міtigація:**
- Створити окремий `SoloWorkspaceV2.vue` без classroom залежностей
- Або адаптувати BoardDock для підтримки Solo v2 API

### 2. Backward compatibility даних
**Ризик:** Старі сесії не мають `background`, `arrow` полів

**Міtigація:**
- Типи вже optional
- Backend зберігає JSONB blob (не валідує структуру)
- Fallback до default значень (white background, arrow-end)

### 3. Продуктивність
**Ризик:** Нові features (selection, background patterns) можуть сповільнити canvas

**Міtigація:**
- Використовувати `useCanvasOptimization.ts`
- Konva caching для складних елементів
- Lazy rendering для patterns
- Performance metrics в dev mode

### 4. i18n
**Ризик:** Нові UI елементи можуть мати hardcoded текст

**Міtigація:**
- Аудит всіх нових компонентів на hardcoded strings
- Додати i18n ключі для нових features
- Запустити `npm run i18n:check`

---

## 📝 CHECKLIST ДЛЯ ВИКОНАННЯ

### Pre-migration
- [ ] Створити backup гілку `solo-v1-backup`
- [ ] Створити робочу гілку `solo-v2-migration`
- [ ] Зберегти старі файли в `docs/SOLO_v2/solo_v1_backup/`
- [ ] Переконатися, що всі тести зелені на поточній версії

### Фаза 1
- [ ] Встановити `pdfjs-dist@4.0.379`
- [ ] Скопіювати 6 нових composables
- [ ] Оновити `types/solo.ts`
- [ ] Додати `BackgroundPicker.vue`, `PdfImportButton.vue`
- [ ] Запустити `npm run typecheck`
- [ ] Запустити `npm run test:unit`
- [ ] Запустити `npm run dev` - переконатися, що старий код працює

### Фаза 2
- [ ] Оновити `store/soloStore.ts`
- [ ] Оновити `toolbar/SoloToolbar.vue`
- [ ] Оновити `toolbar/ToolButton.vue`
- [ ] Оновити `toolbar/ColorPicker.vue`
- [ ] Оновити `toolbar/SizePicker.vue`
- [ ] Оновити `canvas/SoloCanvas.vue`
- [ ] Адаптувати `views/SoloWorkspace.vue`
- [ ] Додати обробники нових events
- [ ] Додати state для нових props (arrowStyle, arrowSize, background)
- [ ] Запустити `npm run typecheck`
- [ ] Запустити `npm run test:unit`
- [ ] Мануальне тестування всіх інструментів:
  - [ ] Pen
  - [ ] Highlighter
  - [ ] Eraser
  - [ ] Line
  - [ ] Arrow (3 стилі)
  - [ ] Rectangle
  - [ ] Circle
  - [ ] Text
  - [ ] Note
  - [ ] Select (rectangle, lasso, move, resize)
- [ ] Тестування background (6 типів)
- [ ] Тестування keyboard shortcuts (22 комбінації)
- [ ] Тестування undo/redo
- [ ] Тестування autosave

### Фаза 3
- [ ] Скопіювати E2E тести
- [ ] Запустити `npm run test:e2e`
- [ ] Performance аудит (autosave debounce, canvas optimization)
- [ ] Memory leak перевірка
- [ ] Оновити README.md
- [ ] Оновити CHANGELOG.md
- [ ] Створити MIGRATION_GUIDE.md
- [ ] Запустити `npm run i18n:check`
- [ ] Code review
- [ ] Merge в main

### Post-migration
- [ ] Моніторинг продакшн метрик
- [ ] Збір фідбеку від користувачів
- [ ] Видалити backup файли (через 2 тижні)

---

## 🎓 ВИСНОВОК

### Чи можна просто скопіювати файли?
**НІ.** Drop-in replacement неможливий через:
1. Зміни в інтерфейсах компонентів (нові props/events)
2. Відсутність нових composables
3. Архітектурна несумісність з Classroom
4. Відсутня залежність pdfjs-dist

### Що потрібно зробити?
**Поступова міграція в 3 фази:**
1. Підготовка (додати нові файли) - 4-6 годин
2. Оновлення core (замінити компоненти + адаптувати батьківські) - 12-16 годин
3. Тестування та фіналізація - 6-8 годин

**Загальний час:** 22-30 годин роботи

### Чи готовий Solo v2 до інтеграції?
**ТАК, але з застереженнями:**
- ✅ Код написаний якісно
- ✅ Документація повна
- ✅ Backward compatible типи
- ⚠️ Потребує адаптації батьківських компонентів
- ⚠️ Потребує тестування
- ⚠️ Потребує додавання залежності

### Рекомендація
**Виконати міграцію поетапно** згідно з планом вище. Кожна фаза має чіткі критерії успіху та можливість rollback. Це забезпечить безпечну інтеграцію без ризику для існуючої функціональності.

---

**Автор аудиту:** Cascade AI  
**Дата:** 2026-02-03  
**Версія:** 1.0
