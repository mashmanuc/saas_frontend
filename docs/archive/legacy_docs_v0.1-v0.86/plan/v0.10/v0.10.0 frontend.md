<!--  -->FRONTEND — TECHNICAL SPEC v0.10.0
1. WebSocket Client Layer

Створити модуль: src/services/realtime/

1.1. Підтримка каналів:

chat

board

presence

notifications

1.2. Автоматичні механізми:

reconnect з експоненційною затримкою

heartbeat

offline-indicator

global WS error handler

2. Realtime Chat UI
Обов’язково реалізувати:

message list з автоскролом

“typing…” indicator

read states

редагування/видалення власних повідомлень

підтримка markdown/MathJax

3. Online Status Indicators

зелена точка = online

сіра точка = offline

у списках тьюторів/студентів

у chat header

4. Collaborative Whiteboard (MVP)
Обов’язкові функції:

олівець (pencil)

гумка (eraser)

undo/redo

кольори

курсор учасників

autosave snapshot (отримувати від BE)

UI:

панель інструментів

поле дошки

індикатор “X учасників у сесії”

5. Notifications Real-Time

показувати push з WS

fallback на HTTP при reconnect

індикатор непрочитаних

зв’язати з поточними email-шаблонами

6. E2E (Playwright)

Обов’язкові сценарії:

студент + тьютор → урок → чат → повідомлення відображається в обох

дошка → малювання → зміни з’являються в іншого користувача

вихід з мережі → offline indicator → reconnect

нотифікації в реальному часі

🟩 Обов’язковий результат версії v0.10.0

До релізу 10.0.0 має бути:

🔥 Full realtime core:

WebSocket gateway

chat

presence

collaborative board

realtime notifications

🧪 Tests:

backend 80+% coverage по realtime

frontend e2e smoke проходить без збоїв