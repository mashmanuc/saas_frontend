1) Створити структуру UI для Classroom Dashboard
🎯 Мета:

Підготувати основу інтерфейсу, де тьютор бачить список класів.

🎯 Детальні задачі:

Створити новий маршрут у фронті:

/dashboard/classrooms

Підключити сторінку до Pinia store (або Vue composable), який зберігатиме:

список класів,

статус завантаження (loading/error/success).

Зробити базовий layout сторінки:

заголовок "Мої класи",

кнопка "Створити клас" (поки без функціоналу).

Контейнер для списку класів:

карточка класу (title + count студентів),

skeleton-loader (3–4 блоки) на час завантаження.

Налаштувати адаптив:

desktop — grid 3–4 колонки,

tablet — 2,

mobile — 1.

Створити компонент ClassroomCard.vue:

props: { id, title, studentsCount }

onClick → маршрутизація до /dashboard/classrooms/:id

2) Інтегрувати API: GET /api/tutor/classrooms/
🎯 Мета:

Підтягувати реальні дані з бекенду.

🎯 Детальні задачі:

Створити api/classrooms.js або composable useClassroomsApi().

Додати функцію:

getTutorClassrooms() → Promise<Classroom[]>


Обробити помилки HTTP:

401 → перенаправити на /login,

500 → показати toast "Помилка сервера".

У ClassroomStore створити actions:

loadClassrooms()

refreshClassrooms()

Виводити реальні дані на UI.

Обов’язково протестувати:

коли класів немає (empty state),

коли є багато,

коли сервер повертає 500.

3) Додати сторінку Classroom Details

Маршрут: /dashboard/classrooms/:id

🎯 Детальні задачі:

Створити сторінку ClassroomDetailsView.vue.

Додати заглушку "Завантаження..." + skeleton.

Звернутися до API:

GET /api/tutor/classrooms/:id/

Відобразити:

Назва класу,

Кількість студентів,

Список студентів.

Створити компонент StudentListItem.vue:

avatar (ініціали),

ім’я або email,

піктограма статусу (active / pending).

Перевірити адаптив.

4) Інтегрувати API: GET /api/tutor/classrooms/:id/
🎯 Мета:

Підключити реальні студентські дані.

🎯 Детальні задачі:

В API-файл додати функцію:

getClassroomDetails(id) → { id, title, students[] }


У ClassroomStore додати:

currentClassroom,

loadClassroomById(id).

Обробка станів:

loading,

error,

404 → показати "Клас не знайдено",

403 → "Немає доступу".

Протестувати:

Якщо студентів немає → показати empty-state:
"У цьому класі поки немає студентів"

5) Додати UI для Classroom → Invite Student
🎯 Мета:

Підготувати інтерфейс для запрошення студентів (бекенд буде у v0.6–0.7).

🎯 Детальні задачі:

Кнопка "Запросити студента" у ClassroomDetails.

Клік → модальне вікно InviteStudentModal.vue.

У модальному:

поле email з валідацією (regex, no spaces),

кнопку "Надіслати запрошення",

кнопку "Скасувати".

Додати UI стани:

loading,

success ("Запрошення надіслано"),

error ("Користувач уже існує" / "Некоректний email").

Сам відправлення пока заглушка:

await fakeApiDelay(1200)
return { success: true }


Після success закрити модальне та показати toast.

6) Оновити дизайн-систему для Classroom-модуля
🎯 Мета:

Зробити класи в єдиному стилі — красиво, технологічно, відчуття “платформи”.

🎯 Детальні задачі:

Оновити змінні теми:

кольори статусів,

spacing,

тіні,

картки.

Додати компоненти:

Badge.vue (для статусу студента),

Avatar.vue (ініціали),

Card.vue (єдиний дизайн для classroom).

Оптимізувати:

typography tokens: h1, h2, body, caption.

Винести всі UI-патерни у /components/ui/…

Забезпечити підтримку Light/Dark themes.

Перевірити accessibility:

контраст,

focus states,

aria-labels.