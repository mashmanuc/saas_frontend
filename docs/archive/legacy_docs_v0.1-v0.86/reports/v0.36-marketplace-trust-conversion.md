# v0.36 — Marketplace Trust + Conversion (Frontend)

## 🎯 Goal
Побудувати перший інкремент "trust + conversion" для маркетплейсу поверх v0.35: реальні сертифікати з файлами і модерацією, публічні відгуки/рейтинги/бейджі, а також прокачаний каталог з новими фільтрами та сортуванням. Фінальний результат має проходити `vue-tsc`, відповідати BE контракту і бути продукційно готовим.

---

## ✅ Що доставили

### 1. Сертифікати (owner UI)
- Новий `CertificationsEditor.vue` (upload → presign → PUT → attach).
- Інтерфейс показує статуси `pending/approved/rejected`, `rejection_reason`, toggle `is_public`, delete.
- Прогрес-індикатор на PUT, ручні валідації типу/розміру, обробка 422/429.
- API-клієнт `marketplaceApi` отримав повний CRUD + presign типи.

### 2. Публічний профіль (довіра)
- `ProfileReviews.vue` + `CreateReviewModal.vue` підключені у `TutorProfileView.vue`.
- Кнопка "Залишити відгук" показується лише залогіненим студентам, сабміт блокує повторне відправлення.
- Після створення відгуку список робить `reload()`. Rating summary і бейджі відмальовуються у `ProfileHeader`/`ProfileBadges`.

### 3. Каталог (conversion)
- `CatalogFilters` підтримує `experience_min/max`, `direction`, `format`, `has_certifications` (checkbox).
- `CatalogSort` має режими `recommended (default)`, `rating`, `price_{asc,desc}`, `experience_desc`.
- `TutorCatalogView` тепер використовує `useMarketplace()` → URL ↔ filters ↔ sort синхронізовані.
- `marketplaceApi.getTutors` має дефолт `sort='recommended'` і чистить `q` < 2 символів.

### 4. Інфраструктура
- Локальний `tsconfig` + `@types/node`, `vue-tsc --noEmit` зелені.
- Встановлено реальний `chart.js`, прибрано тимчасовий shim.

---

## 🧪 Smoke / Go-No-Go
1. **Certifications**: presign → PUT → attach → статус `pending`, toggle visibility + delete працюють.
2. **Reviews**: створення відгуку через модалку, подвійний сабміт блокується (disabled button, state guard).
3. **Catalog**: ручні перевірки `experience_min/max`, `direction`, `format`, сорти — бекенд приймає очікувані query, URL оновлюється, відповіді оновлюються.
4. **Typecheck**: `npm run typecheck` → без помилок.

---

## 📂 Основні файли
- `src/modules/marketplace/components/editor/CertificationsEditor.vue`
- `src/modules/marketplace/api/marketplace.ts`
- `src/modules/marketplace/components/profile/ProfileReviews.vue`
- `src/modules/marketplace/components/profile/CreateReviewModal.vue`
- `src/modules/marketplace/views/TutorProfileView.vue`
- `src/modules/marketplace/components/catalog/CatalogFilters.vue`
- `src/modules/marketplace/views/TutorCatalogView.vue`
- `src/modules/marketplace/composables/useMarketplace.ts`

---

## 🔭 Наступні кроки (optional)
- UX polish для публічного профілю (скрол до reviews, skeleton state).
- Перевести сертифікати на drag&drop + багатофайловий upload.
- Додати e2e-тести на smoke сценарії (cert upload, review creation, catalog filters).
