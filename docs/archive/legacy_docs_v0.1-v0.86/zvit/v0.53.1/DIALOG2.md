# DIALOG2 - Питання до користувача

## Статус аудиту календаря (26.12.2025, 23:47)

### ✅ Що перевірено

1. **Календар тьютора доступний** - http://localhost:5173/booking/tutor
2. **Відображення слотів працює** - жовті блоки availability видно
3. **Редагування слотів** - модалка відкривається при кліку
4. **Backend endpoints існують**:
   - ✅ `POST /booking/slots/custom/` (CustomSlotView) - для створення слотів
   - ✅ `PUT /v1/booking/slots/{id}/` (SlotDetailView) - для редагування
   - ✅ `DELETE /v1/booking/slots/{id}/` - для видалення
   - ✅ `POST /v1/booking/slots/check-conflicts/` - перевірка конфліктів

### ❓ Критичне питання: Як саме користувач створює НОВІ слоти доступності?

**Проблема:** Я знайшов код для `CreateSlotModal.vue` та API endpoint `/booking/slots/custom/`, але **НЕ ЗНАЙШОВ** UI-кнопку або механізм, який дозволяє користувачу відкрити цю модалку для створення нового слоту.

**Що я бачу на сайті:**
- ✅ Жовті блоки (існуючі availability slots)
- ✅ Кнопка "Налаштувати години для бронювання" (веде на іншу сторінку)
- ✅ Кнопка "До першого вільного слоту"
- ❌ **НЕМАЄ** кнопки "Додати слот" / "Створити доступність" / "+"

**Що я знайшов у коді:**
```vue
<!-- CalendarWeekView.vue має CreateSlotModal -->
<CreateSlotModal
  v-if="showCreateSlotModal && createSlotData"
  :date="createSlotData.date"
  :start="createSlotData.start"
  :end="createSlotData.end"
  @created="handleSlotCreated"
/>
```

**Але showCreateSlotModal активується тільки через:**
1. `handleCreateSlot(data)` - але ніде не викликається з UI
2. `handleCellsSelected` в CalendarBoard - drag-select механізм

### 🔍 Питання до вас:

1. **Чи має бути кнопка "Додати слот" на головному календарі?**
   - Якщо ТАК - де саме? (toolbar, floating button, context menu?)
   - Якщо НІ - як тоді користувач створює слоти?

2. **Чи працює drag-select для створення слотів?**
   - Я бачу код для drag-select у `CalendarBoard.vue`
   - Але не впевнений, чи він активний і чи користувач про це знає

3. **Чи "Налаштувати години для бронювання" має створювати слоти?**
   - Зараз ця кнопка веде на `/booking/availability` (AvailabilityEditor)
   - Це окремий інтерфейс для bulk-створення слотів через template
   - Чи це єдиний спосіб створення слотів?

### 📋 Наступні дії (чекаю вашої відповіді)

**Варіант A:** Якщо drag-select має працювати:
- Перевірю, чи він активний
- Додам візуальні підказки для користувача
- Протестую створення слотів через drag

**Варіант B:** Якщо потрібна кнопка "Додати слот":
- Додам кнопку на toolbar календаря
- Підключу до існуючої CreateSlotModal
- Протестую весь flow

**Варіант C:** Якщо слоти створюються тільки через AvailabilityEditor:
- Перевірю, чи працює bulk-створення
- Протестую синхронізацію з календарем
- Задокументую цей flow

### ⚠️ Інші виявлені проблеми

1. **Модалка редагування не закривається** після збереження (з попередніх звітів)
2. **400 Bad Request** при збереженні слотів (потребує детального логу)
3. **Hover-кнопки** (edit/delete/block) іноді не з'являються
4. **i18n warning**: `availability.status.available` відсутній

---

**Будь ласка, вкажіть:**
- Як саме користувач має створювати нові слоти доступності?
- Чи є якісь приховані механізми (drag-select, context menu), про які я не знаю?
- Чи потрібно додати нову кнопку/UI для створення слотів?
