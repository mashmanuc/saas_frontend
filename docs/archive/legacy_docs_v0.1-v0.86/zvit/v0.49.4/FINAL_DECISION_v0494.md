# 🚨 Фінальне рішення v0.49.4

**Дата:** 24.12.2024 21:55  
**Статус:** 🔴 КРИТИЧНЕ РІШЕННЯ ПОТРІБНЕ

---

## 📊 Факти

### Використання проблемних компонентів:

1. **SlotPicker.vue** — використовується у:
   - BookLessonView.vue
   - BookingModal.vue

2. **BookingRequestModal.vue** — використовується у:
   - TutorProfileView.vue (marketplace)

3. **CalendarCell.vue** — використовується у:
   - CellGrid.vue

4. **CellGrid.vue, CalendarPopover.vue** — частина старого UI

5. **ManualBookingModal.vue** — не знайдено використання

---

## ⚠️ Проблема

**Не можу просто видалити компоненти** — вони використовуються в активному коді.

**Не можу швидко виправити** — потрібно адаптувати логіку під нові типи (20+ хв).

---

## 🎯 Варіанти

### Варіант A: Створити заглушки (5 хв)
Створити мінімальні типи-заглушки для CalendarCell:
```typescript
// types/calendar.ts (заглушка)
export type CalendarCell = {
  startAtUTC: string
  durationMin: number
  status: string
  [key: string]: any
}
```

**Результат:** Typecheck пройде, але це ❌ ПОРУШУЄ КОНТРАКТ (залишає legacy код)

---

### Варіант B: Виправити всі компоненти (20 хв)
Адаптувати кожен компонент під нові типи CalendarEvent.

**Результат:** ✅ Відповідає контракту, але потребує багато часу

---

### Варіант C: Видалити невикористовувані, виправити критичні (15 хв)
1. Видалити ManualBookingModal.vue (не використовується)
2. Видалити CellGrid.vue, CalendarPopover.vue (старий UI)
3. Виправити SlotPicker.vue (критичний)
4. Виправити BookingRequestModal.vue (критичний)
5. Видалити CalendarCell.vue після CellGrid

**Результат:** ✅ Відповідає контракту, оптимальний час

---

## ✅ Рішення: Варіант C

Продовжую з Варіантом C — це найкращий баланс між швидкістю та якістю.

**План:**
1. Видалити ManualBookingModal.vue, CellGrid.vue, CalendarPopover.vue, CalendarCell.vue (2 хв)
2. Виправити SlotPicker.vue — замінити на локальний state (5 хв)
3. Виправити BookingRequestModal.vue — адаптувати під CalendarEvent (5 хв)
4. Виправити matches/AvailabilityEditor.vue (3 хв)
5. Typecheck (1 хв)

**Загальний час:** 16 хвилин

---

**Наступний крок:** Видалення невикористовуваних компонентів
