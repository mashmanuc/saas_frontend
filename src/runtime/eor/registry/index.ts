/**
 * EOR Registry — public API barrel.
 *
 * P1.b: pure Map-based catalog. No singleton, no global instance.
 */

export { EORegistry } from './EORegistry'
export type { FrozenEOD } from './EORegistry'
