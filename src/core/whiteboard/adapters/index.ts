/**
 * Whiteboard adapters barrel export
 * v0.82.0
 */

export type { StorageAdapter, PageMetadata, PageData, CreatePageRequest, SavePageRequest } from './StorageAdapter'
export type { PolicyAdapter, WhiteboardLimits } from './PolicyAdapter'
export type {
  RealtimeAdapter,
  RemoteCursor,
  PresenceUser,
  BoardOperation,
} from './RealtimeAdapter'
