Фіналізований план v0.6 (UNIFIED PROFILE MODULE)

З урахуванням усіх правок бекенда.

🟦 1. Фронтенд — уточнений план (frontend06)
✅ 1.1. Profile Store — розмежування з authStore
НОВА ВИМОГА:

useProfileStore відповідає ТІЛЬКИ за дані з /api/me/profile/:

user (read-only)

profile

settings

avatarUrl

authStore і надалі зберігає:

access token

refresh token

role (student/tutor/admin)

login/logout/refresh-flow

Мета:

❗ Повна відсутність дублювання стану.
❗ Minimal coupling між auth vs profile.

✅ 1.2. AvatarUpload — чіткий контракт
Формат запиту:
POST /api/me/avatar/
Content-Type: multipart/form-data

FormData:
  avatar: File

Компонент передає:
emit("upload", file: File)
emit("delete")

Мета:

❗ Гарантована сумісність з бекендом.
❗ Відсутність magic у компоненті — тільки UI та File handling.

✅ 1.3. Settings Page — інтеграція з i18n
Поведінка:

зміна мови в UI → негайно перемикає i18n локально,

паралельно викликається PATCH → /api/me/profile/ або /api/me/settings/,

store оновлює local state.

SettingsStore обробляє:

ui_language

notifications_enabled

timezone

privacy_public_profile

Мета:

❗ Фронт моментально реагує.
❗ Бекенд синхронізує стан у базі.

✅ 1.4. Навігація і guard’и

Необхідно додати до меню:

Маршрути тьютора та студента:

/dashboard/profile

/dashboard/profile/edit

/dashboard/profile/settings

У RouterGuard:

перевіряти роль у authStore.role,

якщо профіль ще не завантажено — викликати profileStore.loadProfile().

Мета:

❗ UX консистентний.
❗ Немає race-condition між auth і profile.

✅ 1.5. Фінальна структура Profile Module
modules/profile/
  api/
    profile.js
  store/
    profileStore.js
  views/
    ProfileOverviewView.vue
    ProfileEditView.vue
    ProfileSettingsView.vue
  components/
    AvatarUpload.vue
    ProfileCard.vue
    ProfileForm.vue


Мета: домен-профіль відокремлений. Готовий до розширення у Marketplace.

технічні завдання для логування (v0.6)

Фронтенд НЕ створює сам логи, а ТРИГЕРИТЬ їх через API.
Тобто логування відбувається автоматично через бекенд.

Але фронту потрібні задачі, щоб правильно запускати логування та працювати з логами користувача.

🟠 FE-620 — Оновити profileStore для логованих операцій

Після успішних:

updateProfile()

updateSettings()

uploadAvatar()

deleteAvatar()

→ store не мусить робити нічого особливого, але повинен точно передавати ті поля, які бекенд очікує.

Мета:

Щоб metadata логів була повною.

🟠 FE-621 — Створити API для отримання активності користувача

/src/api/activity.js:

getMyActivity({ limit=20, before=null, action=null })

Мета:

Щоб фронт міг відображати логування в майбутньому (не обов’язково в v0.6).

🟠 FE-622 — Додати сторінку (або приховане debug view)

Файл:

modules/profile/views/ProfileActivityView.vue


Поки що:

список подій

action

timestamp

metadata (JSON expand)

Це опціонально, але дає фахівцю UI можливість бачити логи в dev/stage.

Мета:

Справа на майбутнє — analytics dashboard.

🟠 FE-623 — Підготовка до marketplace tracking

В ProfileOverview:

При відкритті сторінки, frontend НЕ логуватиме подію — це робить бекенд на рівні каскаду (ми це зробимо у v0.7).

Поки що задача:

чітко відокремити profileStore від dashboardStore

щоб у майбутньому було легко вбудувати кліки/взаємодії.