TЗ Frontend — v0.9.1 Patch Release
🎯 Ціль

Закрити всі дрібні, але важливі задачі, які потрібні для повної відповідності бекенд-контрактам і завершення модуля Relations + Lessons.

1. Завершення Relations Dashboard (пункт 1 із 9.0)
1.1. Unit Tests (обов’язково)

Потрібно написати:

тест на cursor pagination

тест на bulk accept/archive

тест на offline / 429 помилки

тест на empty-states

тест на loadMore поведінку

тест на перехід invited → active → archived

1.2. Component Tests (DashboardTutor.vue)

Покрити:

відображення вкладок

масові дії

статусні бейджі

error banners

1.3. QA Checklist (Relations)

Документувати:

усі переходи станів

поведінку при перезавантаженні сторінки

текстові стани (локалізації)

2. Lessons Module — завершення узгодження з BE

Оскільки бекенд вніс зміни у v0.9.1:

2.1. Додати підтримку series_id (зараз порожнє)

Фронт має:

читати series_id

відображати у списку

поки НЕ створювати серії (це у v0.11)

2.2. Виправити timezone fallback

Коли бекенд виправить локалізації — фронт має:

протестувати всі timezone,

додати unit-тести для переходів UTC ↔ local.

2.3. Edge-case тести

reschedule cancelled lessons

overlapping lessons (бекенд повертає помилку — фронт має інформувати)

3. i18n Cleanup (для закриття 9.0)
3.1. Закрити всі missing keys warnings

(зокрема: lessons.calendar., relations.bulk., timezone.*)

3.2. Додати локалізації для Dev Theme Playground

(бо зараз частина компонентів англійською)

4. Dev Theme Playground — мінімальні виправлення

Оскільки Playground вже створено, у 9.1 потрібно:

додати Sidebar навігацію між компонентами

вирівняти заголовки

вимкнути доступ для учнів/тьюторів (залишити лише admin)

додати блок "Design Tokens Inspector"

📌 Підсумок v0.9.1 Frontend Patch
Блок	Статус	Примітка
Relations — unit tests	❗ треба виконати	блокер для freeze
Relations — UI tests	❗ треба виконати	блокер
Lessons — timezone & BE sync	❗ треба виконати	пов’язано з BE 9.1
i18n cleanup	бажано	косметика, але важливо
Dev Playground fixes	бажано	закриває борги 9.0